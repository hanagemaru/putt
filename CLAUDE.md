# パッティングゲーム

スマホ縦画面・ブラウザで動く一人称パッティングゲーム。hanage.app で公開予定。

**作業開始時はまず `PROJECT_STATUS.md` で現在地・実機確認待ち・次の作業を確認する。** 初回公開までの工程・依存関係・完了条件は `RELEASE_PLAN.md`、詳細仕様は `docs/spec.md`、実装履歴と長いタスク分割は `TASKS.md`。このファイルには恒久的な前提だけを書く。作業中のメモは書かない。

## スタック

- Vite + TypeScript + three.js（物理エンジンは使わない）
- 調整UIは lil-gui
- 依存は最小限に。新しいライブラリを入れる前に必ず確認を取ること

## 座標系と単位

- three.js 標準の Y-up。グリーンは XZ 平面、高さが Y
- **単位は常にメートル・秒**。ピクセルとメートルの変換は `src/config.ts` の一箇所だけで行う
- 角度は内部ではラジアン、UI表示のみ度

## 設計上の約束

- **チューニング可能な数値は全て `src/config.ts` に置く。** 他のファイルにマジックナンバーを書かない
- 物理は固定タイムステップ 1/240 秒。決定論的に保つ（後でリプレイに使う）
- コースの計算領域は正方形に固定せず、**幅（X）と長さ（Z）を別々に持つ長方形**とする
- ハイトマップ解像度は固定点数ではなく目標セル間隔から寸法別に決め、長いホールにも同程度の精度で対応する
- 正式仕様では、コースのハイトマップとサーフェスマップを**表示と物理の両方の唯一の情報源**にする。見た目と挙動を別々に持たない
- 視認性検証のために見た目だけを誇張する場合は、**比較用であることを明示し、正式仕様と混同しない**

## ゲーム設計の原則（変えないこと）

- **長いライン予測・傾斜を含む曲がり予測は出さない。** ADDRESS方向調整・LOW_LINE・STROKEカップ確認では、打ち出し方向を示す約0.5mの短い直線だけ表示してよい
- **STROKE 中にホールを強制表示・非表示しない。** 近距離で自然に視野へ入る場合は、そのままカップと旗竿が見える
- ボールが転がっている最中に俯瞰へ切り替えない。分析は停止後
- グラフィックは簡素でよい。リアルなのは体験であって見た目ではない
- **縦画面専用。** 横画面にさせない（遊ぶ側の一手間を増やさない）
- 基本ルールは打数を競うストロークプレー。通常ツアーは固定9ホール、週替わりチャレンジは毎週月曜0時（日本時間）更新の全員共通3ホールで挑戦回数は無制限
- 通常ツアーと週替わりチャレンジは自己ベストとオンラインランキングを持つ。練習モードはランキング対象外
- 広告は3ホール終了時、およびラウンド途中から別のニューゲームを始める直前に表示する。同じ遷移で二重表示しない

## 検証

- 変更したら `npm run dev` が通ることを確認
- **操作感に関わる変更は実機確認が必要。** その場合は「実機で確認してください」と明示して止まること。自分で「良くなった」と判断しない
- **main への push で GitHub Pages に自動デプロイされる。** ブランチごとのプレビュー環境はない。実機で確認するにはマージが要る
  - ゲーム本体: `https://hanagemaru.github.io/putt/`
  - スワイプ計測: `https://hanagemaru.github.io/putt/swipe-test/`

## やらないこと

- 指示していないリファクタリング
- テストコードの追加（今は不要）
- README や docs の自動更新
- 複数タスクの同時実行。1セッション1タスク
