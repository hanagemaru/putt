# 現在地 / Project Status

最終更新: 2026-09-20（**コースごとの雰囲気（テーマ）と木。2回目の実機で値を確定し、OB境界の指摘3件も直して3回目待ち**。
見た目の調整パネル `?tune=1` を追加。
OB境界の線・カップぎわのけられ・旗竿の反発は確定済み。**オンラインランキングは公開済み**。
残るはハブのプライバシーポリシー差し替えと週替わりの板）

このファイルは **Claude / ChatGPT / Codex 共通の短い引き継ぎ用「現在地」**です。
新しいセッションでは、長い履歴を最初から読み直す前にまずここを確認します。

## ファイルの役割

- `PROJECT_STATUS.md`（このファイル）: 今どこまで終わっているか、実機確認待ち、次にやることを短く保持する
- `RELEASE_PLAN.md`: AdSense審査掲載の中間マイルストーンと、Putt初回公開までの工程・依存関係・完了条件
- `docs/PLAYTEST_BACKLOG.md`: 実機で気になったが、まだ正式仕様として確定していない改善候補
- `CLAUDE.md`: Claude向けの入口。技術・設計上の恒久ルール
- `AGENTS.md`: ChatGPT / Codex向けの入口。上記と同じ正本へ誘導する
- `TASKS.md`: 実装済みの履歴と、より長いタスク分割・実装順
- `docs/spec.md`: ゲーム仕様・UX・数値などの詳細な正本
- `docs/pwa.md`: PWA（マニフェスト・アイコン・Service Worker）の構成と実機で見るところ

同じ仕様を複数ファイルへ大量に複製しない。矛盾した場合は、ゲーム仕様は `docs/spec.md`、恒久ルールは `CLAUDE.md`、現在地はこのファイルを優先して確認する。

## 現在の最優先方針

**Puttの完全完成をAdSense審査の前提にはしない。**
Multicolor Sweeperを審査の主力とし、Puttは「通常ツアーが開始から終了まで安定して遊べる状態」で掲載できるようにする。

**この方針で必要だった作業（1〜5）は2026-09-10に完了した。**

1. ~~ゲーム内から `https://hanage.app/privacy/` を開ける導線を追加する~~ 完了
2. ~~トップから簡単な遊び方を確認できるようにする~~ 完了
3. ~~未接続の週替わりチャレンジは、審査時の公開トップから「準備中」表示を一旦隠す~~ 完了
4. ~~OB境界など、通常プレイに直接支障する分かりにくさが残る場合だけ小さく直す~~
   実機確認で改善要求が出なかったため着手しない（P0-2）
5. ~~公開URL・主要端末・hanage.appからの導線を確認し、AdSense審査へ進む~~
   確認は完了。**申請そのものはPutt側の作業ではない**
6. 審査を待たせない改善は `docs/PLAYTEST_BACKLOG.md` に残し、後続で進める（継続）

**Putt側に審査掲載のブロッカーは残っていない。**
AdSense申請全体・ハブ側の準備は `hanage-hub` の正本に従う。

## 現在地

- **コースごとの雰囲気（テーマ）と木の作り直し: 実装済み・実機確認待ち**
  （`docs/PLAYTEST_BACKLOG.md` §12 / §11）
  - **4コースが同じ絵だったのをやめた。** 空・光の強さ・地面の色・木を
    コースごとに持たせる器（`CONFIG.themes` ＋ `TourDefinition.theme`）を作り、
    **朝の草原（BEGINNER）/ 昼の林間（STANDARD）/ 高原の午後（ADVANCED）/
    夕方のリンクス（EXPERT）** を入れた
  - **雰囲気はコースの中身と矛盾しない割り当てにした。** 池が1つも無い BEGINNER は
    水辺の絵にせず、池が最も多く最も狭い EXPERT を海沿いにした。
    §12 の「名前が風景を約束すると破れる」の裏返しで、**名前では約束していない**
  - **高さの読み（明暗差）を最優先で守った。** 芝3種は相対輝度
    （芝179 / ラフ130 / セカンドカット98）から ±8 以内に収め、**色相と彩度だけ**振った
  - **光の向きは4テーマとも変えていない。** 低い夕日は雰囲気が出るが、斜面の陰影が強まって
    「明るい＝高い」と競合する。**提案時は EXPERT / ADVANCED で光を低くするつもりだったが取りやめた。**
    触るのは強さ（1.65〜1.80 / 0.50〜0.62）と**環境光の色だけ**
    （環境光は全頂点へ同じ倍率で掛かるので明暗の比が変わらない）
  - **木を円柱＋円錐から作り直した**（§11）。樹種は3つで、**1本ごとに乱数で揺らぐ**
    - **広葉樹**: 丸い塊を3〜5個ずらして重ね、枝を2〜3本出す
    - **針葉樹**: 円錐を3〜4段、段ごとに半径と向きを変えて積む。**段数が違っても木の高さは揃える**
    - **低木**: 組み方は広葉樹と同じで、背が低く横に広い
    - 揺らぐのは葉と幹の色・幹の太さ・葉の広がりと高さ・塊や段の数と半径・枝の本数と向き。
      **色は1本につき1回だけ引く**（部位ごとに振ると1本の木が縞に見える）
    - **幹は鉛直のまま**（傾き表現の基準）。輪郭の崩しは枝と塊でやる
    - 部品は1つのジオメトリへまとめ、**色は頂点カラーへ焼く**。
      1ホールぶんの木が**メッシュ1個**になった（木14本で28個だった）
  - **EXPERT でも木を0本にしていない。** 木は枠内のOBへ置いてあり
    「どこからOBか」の手掛かりを兼ねているので、10本までにとどめた
  - **練習モード・週替わりチャレンジ・検証ページは既定のまま**（テーマを持たない）。
    木の形だけは全モードで新しくなる
  - `?theme=<ID>` でどのコースへも当てて見比べられる
  - **触ったのは見た目だけ。** 生成器・物理・シード・自己ベスト・ランキングは変えていない。
    木の乱数は `createTrees` が自分で作るので生成器とは無関係で、当たり判定も持たない
  - `validate:course` / `validate:challenge` / `check:stuck`（4ツアー・詰み0・決定論OK）を通した
  - **1回目の実機で4件出たので直した（2回目の実機確認待ち）**
    1. **EXPERTが赤すぎる・傾斜のグラデーションが見えづらい** → 先に測った。
       **明るい芝ほど濃淡の上側が白へ張り付く。** 旧芝 `0xa8c95a` は上2段が飽和していた。
       `0x8aa84a` へ落として**飽和ゼロ**にし、画面に出る明るさの幅は 98 → 108 へ広がった。
       空も `0xd9a97f`（橙）→ `0xdcc7a4`（かすんだ砂色）
    2. **幹と葉に隙間** → **正二十面体は面が半径の0.79倍のところにある**のに半径ぶん
       持ち上げていたのが原因。低木は潰し0.6のぶんさらに開いていた。
       1個目の塊を幹へ沈め、持ち上げにも潰しを掛けるようにした
    3. **STANDARD / ADVANCED の木が大きすぎる** → 3.5〜6.5m → 2.8〜4.8m、
       5.0〜8.5m → 3.6〜6.0m
    4. **STANDARDの樹種の混ざり方がわざとらしい** → 1本ずつ乱数で引いていたのをやめ、
       **位置のなめらかなノイズ**で決めるようにした。近くの木どうしが同じ樹種になり、
       実際の林のようにまとまる（まとまりの大きさは `CONFIG.trees.standSize`）
  - **2回目の実機で、調整パネルを使って STANDARD / ADVANCED / EXPERT の値を確定した**
    （BEGINNER は据え置き）。EXPERTの空は `0xa3dbc9`（薄い青緑）、
    木は3コースとも一段低く、針葉樹と低木の形も詰めた。**3回目の実機確認待ち**
  - **STANDARD の環境光は、選ばれた「やわらかさ」を保ったまま総量だけ戻した。**
    実機の 1.75 / 1.32 は**光の総量まで1.5倍**になっていて、
    よく出る高さ帯（濃淡 0.7→1.0）で**高さの差が 33 階調 → 7 階調**へ潰れていた
    （EXPERTで直したのと同じ現象）。
    やわらかさ（環境光が光全体に占める割合）0.55 はそのままに **1.45 / 1.12** へ直し、
    明るくなるぶんは**パレットをリニアで一律 ×0.82** して相殺した。
    画面に出る明暗は本番と同じ 147 → 190 → 222（傾き 32）に戻り、
    見た目だけが「やわらかい」状態になる
  - 低木の「塊の広がり」を 1.05 まで上げたぶん塊が本体から離れうるので、
    **横のずらしに必ず重なる上限**を入れた（`CONFIG.trees.clumpTouch`）。
    20万回引いて確かめたところ、**効くのは広葉樹0.0% / 低木0.1%**、効いたときも平均4%縮むだけ。
    逆に**無いと低木の0.03%が浮く**（1ホール30個として110ホールに1個）。
    見た目は指定どおりのまま、浮きだけが消える
- **OB境界の線: 実機の指摘3件を直した（実機確認待ち）**（`docs/PLAYTEST_BACKLOG.md` §5-1）
  - **濃さを1.5倍**にした（手前 0.18 → 0.27、遠く 0.05 → 0.075）
  - **打った後のカメラでは出さない。** 例外は `FOLLOW`（打った直後の最初のプレイヤー視点）だけで、
    そこでOBに入ったかを見られる。`CUP` と `RESULT` 以降では消える。
    実測で、停止後の俯瞰に出ていた境界の画素は **12959 → 0**
  - **一定間隔の濃い点を消した。** 濃さを8段に丸めて**段ごとに stroke していた**ので、
    段の変わり目で線分の丸い端が重なり、そこだけ2回塗られていた。
    **まとめて1回で stroke する**ようにし、距離による薄まりは
    **画面の縦方向のグラデーション**に置き換えた（段に丸める必要がなくなり、薄まり方もなめらか）
- **見た目の調整パネル: `?tune=1`**（`src/theme-tune.ts`）
  - **実際のコースに立ったまま**、空・光・地面の色・木（本数/高さ/色/樹種の比/形）を動かせる
  - 「テーマを読み込む」で4コース＋既定を切り替え、**「決めた値を出す」で貼れる形に書き出す**
  - **付けたときだけ lil-gui を読み込む**ので通常のプレイには載らない
    （`?tour=expert&tune=1` のように使う）
- **OB境界の線: 確定・マージ済み**（`docs/PLAYTEST_BACKLOG.md` §5-1 / `docs/spec.md` §1）
  - **「色を強くする」では解けないことを先に測った。** 芝の頂点カラーは高さで明暗が
    振れるのにOBは単色なので、本番36ホールの境界 12536 組のうち
    **44.4% は打てる側のほうがOBより暗い**（25.0% は差20未満）。
    「暗い＝OB」がコースの半分で逆を向くので、OBの色をどう選んでも片側は必ず裏切る
  - → **コースに馴染ませない。** 現実の白線を真似るのではなく、
    **方向ガイドと同じ `THREE.Line`** で境界をなぞる。
    WebGLの線は常に1デバイスピクセル幅なので、低解像度ターゲット上では**常に1ドット**。
    距離で太さが変わらず、ライトも受けないので遠くでも陰でも同じ濃さで読める
  - **白杭（1mの円柱）と、頂点カラーの太い白帯も作って見比べ、どちらも取りやめた。**
    杭はマップで点線にしかならず、頂点カラーの帯はマスが0.156mなので太すぎた
  - 形は `surfaceAt` からしか読まない。**生成器にも `CONFIG.course.edgeNoise` にも
    触っていない**ので、既存ホールの外形も自己ベストも変わらない
  - マーチングスクエア＋辺上の二分探索（6回・約4mm）で階段にならない線を作る。
    1ホール375〜404本（最大498）、生成16.2ms
  - **プレビューで「目立ちすぎ」と出たので濃さを3段階落とした**（1.0 → 0.55 → 0.35 → **0.18**）。
    **カメラから遠いほど薄くする**（4m→18mで **0.05**）。
    **コースマップだけは距離で薄くせず全線を出す**
  - **太さの作り方を3つ作って見比べ、`smooth` に決めた（残り2つは消した）。**
    補助線の「滑らか」と同じ高解像度Canvasへ相乗りし、**画面の実解像度で描く**ので
    ドットの粒に縛られず太さを選べる。**代わりに深度を持てないので丘も木も透ける**
  - 数字はプレビューで2回落とした。**太さ 1.5 → 3 px、手前 0.35 → 0.18、遠く 0.10 → 0.05**
  - **「途切れて見える」も直した。** 端点を数えて切り分けたところ、
    枠ぎわで線が開いていたのが **132点 → 0点**（走査の格子を枠の外へ1マス広げた）。
    マップ中ほどの途切れは木の傘で、マップでは `depthTest` を切って繋げた。
    残る12点は池の切れ目で意図どおり
  - **池際には引かない**（OBの印でありハザードの印ではない）。**地形には隠れる**
  - **物理には一切関わらない**。乱数も引かないので木の配置も不変
  - `npm run check:stuck --tour=` を4ツアーとも通した（決定論OK・詰み0）。
    `validate:course` / `validate:challenge` も通過
  - **プレビューで4回見比べて確定。** 濃さは 1.0 → 0.55 → 0.35 → **0.18**、
    太さは 1.5 → **3px**。出し方は3つ作って `smooth` を採った
- **カップぎわのけられと旗竿の反発: 確定・マージ済み**（`docs/PLAYTEST_BACKLOG.md` §14）
  - プレビューで4回見比べ、**`flagstickCaptureSpeed` 2.1 で確定**（2026-09-19）
  - 試遊とプレビューで出た指摘に、2箇所の判定を直して答えた。**設定値は増やしていない**
  - **なめる（リップアウト）の効きに速度を入れた。** 進入位置は元から入っていたが速度が無く、
    入る寸前の球も 4m/s で撃ち抜いた球も同じ角度で曲がっていた。
    口を横切る時間は `弦 / 速度` なので、速い球ほど曲がらないのが正しい。
    横ずれ34mmで、入る寸前 58.0° / 2m/s 32.2° / 4m/s 16.1°
  - **蹴られの最大角度を 60.0° にした**（`cupGrazeTurn` 1.33）。
    出るのは落ちる寸前の遅い球だけで、速い球はほとんど曲がらない
  - **竿の判定を「当たったときの速さ」だけで決める形にした。**
    衝突“後”の速度で見ていたため、反射の仕方が横ずれで変わるぶん入る確率も横ずれに
    強く引きずられ（芯 3.75m/s まで入る一方、竿の縁は 0.70m/s まで）、
    芯では何を打っても入るのに跳ね返りは作れない、という三すくみになっていた
  - 判定を衝突“前”にしたことで、**竿に当たる帯（0〜33.3mm）の上限が横ずれによらず
    一定 2.1 m/s** になり（カップを4.0m過ぎる強さ）、
    `flagstickRestitution` が独立したので 0.3 へ戻せた。
    **強すぎた球は手前へ跳ね返る**（到達 2.2m/s で 0.43m、4.0m/s で 1.34m 手前に止まる）
  - 実測で、横ずれ0〜32mmの上限は**すべて 2.05 m/s**（前は芯 3.75m/s → 竿の縁 0.70m/s）。
    33.3mm から先は竿に触れないので、口の弦で決まる上限（1.00 → 0.35 m/s）へ落ちる
  - 弾かれた球の向きも横ずれで分かれる。**芯寄り（〜18mm）は手前へ戻り、
    20mm より外は接線成分が勝って先へ流れる**
  - **上限はプレビューで 1.6 →（きびしい）→ 3.2 →（入りやすすぎる）→ 2.3 →（まだ少し入りやすい）
    → 2.1 で確定した。**
    11m 手前までならこの速さで到達できるので、ほとんどの距離で「強すぎて弾かれる」が起こりうる。
    **上げすぎると遠い距離で到達自体ができなくなり、竿に当たれば必ず入るようになる**
  - 触ったのは `src/config.ts` の5つの数値と `src/physics.ts` のカップ判定。
    **生成器は変更していない**
  - `npm run check:stuck --seeds=1-20` を v1・v2 の両方で通した（詰み0・カップイン可・決定論OK）
  - **残る確認は、2.1 に決めた後の実機での通し確認だけ**（1.6 / 3.2 / 2.3 は見比べ済み）
- **通常ツアーを生成器v2の4コースへ一新した: 実装済み・実機確認待ち**
  - **v1の3コース（風の丘・曲がりの森・水鏡の庭）は一覧から外した。**
    生成器v1のコード自体は残る（週替わりチャレンジv1が使う）
  - やさしい順に **BEGINNER / STANDARD / ADVANCED / EXPERT**（日本語表示でも同じ英字）

    | | 全長 | 芝幅 | 池 | 砂（面積比） | 岸なしの池 | 総回頭角 | 遠回り率 最大 |
    |---|---:|---:|---:|---:|---:|---:|---:|
    | BEGINNER | 152m | 5.17m | 0個 | 8個 (1.1%) | 0本 | 27° | 1.03 |
    | STANDARD | 185m | 4.26m | 6個 | 18個 (3.5%) | 4本 | 44° | 1.06 |
    | ADVANCED | 207m | 4.06m | 4個 | 20個 (4.3%) | 2本 | 50° | 1.27 |
    | EXPERT | 213m | 3.85m | 8個 | 23個 (3.3%) | 6本 | 84° | **1.39** |

  - **BEGINNER は実機で「このままでいい」と出たので据え置いた。**
    生成器を触っても既定オプションの出力は1ビットも変わらない
    （シード1〜400のダイジェストで確認済み）ので、シードも仕立てもそのまま

### 2回目の試遊で分かったこと（2026-09-17）と、その直し

- ⚠️ **EXPERT H3 が1ホール15打になった。カップ周り2mの64.5%が「止まれない面」だった**
  （勾配7.8%超＝摩擦より重力が勝つ面）。寄せても止まらない。同じ症状が他に4ホール
  - 原因は**うねりの倍率を1.0より上げたこと**。1.0以下では9ホール中0件、
    1.1で3件、1.2で3件。→ **うねりは BEGINNER 0.8・他 1.0 に固定した**
  - **`check:stuck` はこれを拾えなかった。** 止まれるマスからしか打たないので、
    カップ周りが止まれない面だらけでも合格する。**同スクリプトに検査を足した**
    （`カップ周り2mの止まれない面`。しきい値2%）。シード選定の条件にも入れた
- **池はほとんど難易度に効かない。** 生成器は池をルートから必ず離して置くので、
  実測でルート中心線から芝半幅の1.94倍（フェアウェイ1本分外）にある。
  実際、池8個のADVANCEDより池ゼロのSTANDARDのほうがスコアが悪かった
  - → **難易度は「芝幅 × 長さ × 形 ＋ 砂」で作る**ことにした。池は景色と回り込む理由
  - → 池を少しは効かせるため、**岸（ラフ）を無くしてフェアウェイが直接水に接する**
    ホールを混ぜられるようにした（`bareWaterChance`。STANDARD 0.3 / ADVANCED 0.4 / EXPERT 0.5）
- **バンカーのレイアウトが似たものばかり**だった。当然で、既定は
  半径1.1〜2.0m・ほぼ丸・ルートの後半50〜85%・最大2個しかなかった
  - → `variedBunkers` を足した（半径0.8〜2.2m・細長い形・ルートの1/4目から・最大4個）。
    砂ゼロのホールが15%→11%に減り、個数が1〜3に散る。面積は 最大5.4%→7.7%
  - **半径を3.2mまで広げたら逆効果だった**（狭い芝に置けず砂ゼロが39%に増えた）。
    700シードで測って 0.8〜2.2 に落ち着いた
- **v2のS字が体感できない**（v1のS字は？という指摘）。実測で裏付けられた

    | | 総回頭角 中央 | **遠回り率 中央** | 最大 |
    |---|---:|---:|---:|
    | v1 doubleDogleg | 119.8° | **1.15** | 1.51 |
    | v2 serpentine（従来） | 45.5° | **1.01** | 1.04 |

  - **効いていたのは総回頭角の上限ではなく骨格の制御点の数だった。**
    4点では大きなS字を Catmull-Rom で表せず、曲率が跳ねて作り直しに落ちていた
  - → `wideSCurve` を足した（制御点14・上限300°）。半径×0.5 と合わせると
    回頭 最大231°・遠回り率 最大1.364 まで出る。
    **EXPERT H2 は総回頭角202.7°のS字で遠回り率1.26**（v1と同じ振り幅）
- **ハザードの摩擦がグリーンの速さにつられて変わる問題も直した**（前回）。
  通常芝だけが `stimpFeet` に従い、ラフ・砂は `referenceStimpFeet`（10ft）で固定

### 3コースを「ホールごとに仕掛けを足す」作り方で組み直した（2026-09-18）

実験コース LAB が実機で「かなりいい。固定コースに全部採用したい」となったので、
**その作り方をそのまま STANDARD / ADVANCED / EXPERT の作り方にした。**
LAB の15ホールは3コースへ配り、**LAB という枠は解散**した
（同じシードが2つのコースに出るのを避けるため。また実験したい要素が出てきたら作り直す）。

土台は `tourSetups.composed`（仕掛けゼロ・ティー側の直線35%だけ）で、
ホールごとに `CONFIG.course.holeFeatures` の部品を 0〜3個足す。
**難易度はコース単位のつまみでは作らない。** 3コースとも速さ10ft・うねり1.0・
曲率半径の下限も同じで、違うのはどの仕掛けをいくつ足すかと、芝幅・長さの選び方だけ。

| | 全長 | 芝幅 | 曲がり | 遠回り最大 | 池 | 砂 | 1打目の最短 | ハザードなし |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| BEGINNER | 163m | 4.63m | 25° | 1.03 | 0 | 4 | 87% | 5本 |
| STANDARD | 192m | 4.36m | 28° | 1.03 | 3 | 17 | 82% | 2本 |
| ADVANCED | 210m | 3.65m | 62° | 1.35 | 8 | 13 | 58% | 2本 |
| EXPERT | **230m** | **3.05m** | 66° | 1.26 | 7 | 9 | 41% | 2本 |

**全長も芝幅も4コースで単調**になった（163→192→210→230m ／ 4.63→4.36→3.65→3.05m）。

### 砂がOBへめり込んでいたのを直した（2026-09-19）

砂は長らく**芝の上にしか塗られていなかった**ので、半径がOBへ届いた砂はそこで
切り落とされ、見た目には「砂がOBへめり込む」状態になっていた（実機で EXPERT H1 の指摘）。

**最初は「はみ出す砂を置かない」で直したが、これは失敗だった。**
36ホール中5ホールで砂が減り、EXPERT H1 は3個が0個になってホールの性格ごと消えた。
細い道（OBが中心線から3.3m）とカップ周りの砂は、そもそも幾何学的に両立しないため。

**実機から「置かないのではなく、はみ出た箇所だけOB境界を砂の形に合わせて変える」**
と指摘があり、そちらへ作り直した。これが正しい。

- `surfaceAt` は**帯に関わらず砂を塗る**。OB側へはみ出した分もそのまま砂になる
- 砂の縁の外 `obClearance`（0.3m）ぶんは、OBだった場所をセカンドカットに変える。
  **OB境界が砂の形に沿って膨らむ**ので、砂が直接OBに接することがない
- 枠（`bounds`）の外だけは膨らませられない（ハイトマップが無い）。
  枠に掛かる砂は**置くのをやめる前にまず縮めて収める**（`bunker.minRadius` 0.5m が下限）

結果を突き合わせると、置かない方式より明らかに良い。

| | 砂の総数 | 砂が減ったホール | OBに接する砂マス |
|---|---:|---:|---:|
| 修正前 | 43 | — | 105 |
| 置かない方式 | 36 | **5ホール** | 0 |
| **境界を膨らませる方式** | **44** | **0ホール** | 0 |

**細い道とカップ周りの砂が両立するようになった。** 置かない方式では
25.5m以上のPAR4で0本だった組み合わせが、普通に選べる。
EXPERT H1 だけシードを差し替えた（387 → 1129）。`surfaceAt` が変わるとOB面積比も変わり、
生成器の検証を通る試行がずれて 27.4m → 19.9m の別ホールになってしまったため。
1129 は 27.2m・芝3.04m・OBまで3.39m・砲台17cm・**カップ周りに砂3個（奥2個）**で、
狙いどおりの「砲台＋カップ周りの砂＋細い道」に戻っている。
**他の35ホールはシードも砂の数も変わっていない。**

### コース選択の開始ボタンを「スタート」にした（2026-09-19）

「HOLE 1から」は「HOLE nから再開」と並んだときにしか意味が通らない、と実機で出た。
続きの有無で出し分ける。

- 続きなし: **「スタート」/ START** だけ
- 続きあり: **「はじめから」/ FROM START** ＋ 「HOLE nから再開」/ RESUME Hn

### コース選定は確定。ホール順の決まりを変えた（2026-09-18）

**シードの選び直しはここで打ち止め。** 以降は並び順と見せ方だけを触る。

PAR の並びを**4コースとも同じ型**にした。

    H1  H2  H3  H4  H5  H6  H7  H8  H9
     4   3   5   4   4   3   5   4   4   （合計PAR36）

- **最初と最後は必ずPAR4。** 短いPAR3で始まる／終わるとラウンドの締まりがない
- **PAR5はH3とH7に分ける。** 直す前は3コースで両方とも H8・H9 に固まっていて、
  前半がずっと軽く最後だけ重いラウンドになっていた
- 「最終ホールはそのラウンドの最長」という以前の決まりは**やめた**。
  最長はPAR5（H3かH7）に来るので、最初と最後をPAR4にする決まりと両立しない
- 隣り合うホールで形を繰り返さないのは維持。地形も繰り返さないほうが良いが**形を優先**する
  （ADVANCED は9ホール中5ホールがポテチで、PAR3の位置が固定なので2か所避けられない）

**コース選択の説明文はゲームから消した**（実機の指摘）。名前と自己ベストだけになり、
4枠がスクロールなしで収まる。`TourDefinition.description` は残してあるが、
**いまはマップ一覧（`npm run maps`）の見出しでだけ使う**。

### ⚠️ ローカルの `vite preview` はベースパスが `/putt/`

`npm run build:cloudflare` の直後に `npx vite preview` を開くと、
index.html が `/assets/` を指すのにサーバーは `/putt/assets/` で配信するため**全部404**になる。
このときページは真っ白だが `pageerror` は出ないので、
**「JSエラーなし」だけを見ていると壊れていることに気付けない。**

ローカルで確認するときは `npm run build`（base `/putt/`）を最後に実行し、
`http://localhost:<port>/putt/` を開く。`requestfailed` の数も併せて見ること。
**本番とCloudflareプレビューは正しい**（それぞれのベースパスでビルドして同じパスで配信している）。

### ⚠️ EXPERT は一度 201m で出して失敗した（2026-09-18）

実機で **「体感は順番どおりに難しいが、スコアは ADVANCED より4打良かった」** と出た。

**狭さだけでは打数が増えない。** 狭ければ短く刻んで安全に運べてしまうし、
OBは外さなければ発動しない。**打数に効くのは長さ**。
狭さ（芝3.05m）を保ったまま、各PARの帯の長い側へ寄せて 201m → 230m にした
（PAR3は12.3m以上／PAR4は25.5m以上／PAR5は34.9m以上。帯の上限は 13・27.5・35.5m）。

ADVANCED より20m長く、かつ0.6m狭い。**逆に行き過ぎている可能性があるので、次の実機で見る。**
「スタンダードとアドバンスドを同じ量だけ下げる」案は、伸ばす側で足りたので使っていない。

### BEGINNER は風の丘（v1）といままでのビギナー（v2）の混成にした

2つのセットは性格がはっきり違う。

| | 全長 | 芝幅 | 曲がり | ハザード |
|---|---:|---:|---:|---|
| 風の丘（v1・本番） | 178m | 3.87m | 17° | **池も砂もゼロ**。9本中6本が「むずかしい・ストレート」で似ている |
| ビギナー（v2） | 152m | 5.17m | 27° | 砂8個。短くて広く、少し曲がる |

**似たものを並べないことだけを条件に**9本選んだ（v1から4本・v2から5本）。
v1の「むずかしい・ストレート」は5本もあって互いにそっくりなので2本だけ使う。
PAR5はv2を優先した。**v1とv2でparの境目が違う**ためで（v1は22m以上がPAR5、v2は27.5m以上）、
v1のPAR5は24.7mと短く、同じPAR5でも寄せやすさが揃わない。

**生成器をホール単位で指定できるようにした**（`TourHole.generator`）。
これを使うのはBEGINNERだけ。

#### どのコースにも池も砂も無いホールを2本ずつ入れた

実機の「ハザードなしで幅と形だけで難易度を作るパターンもあったらいい」への答え。

| コース | ホール | 仕掛け | 芝幅 | 中心線からOB |
|---|---|---|---:|---:|
| STANDARD | H1 (940) | 仕掛けなし | 4.76m | 6.1m |
| STANDARD | H9 (1040) | 幅のうねり・PAR5 33.1m | 3.23m | 4.42m |
| ADVANCED | H4 (1433) | くびれ | 5.09m | 6.64m |
| ADVANCED | H5 (516) | 細い道＋幅のうねり | 2.75m | 3.25m |
| EXPERT | H2 (539) | 細い道 | 1.96m | **2.29m** |
| EXPERT | H6 (1315) | 細い道・PAR3 | 3.00m | 3.41m |

#### 選定でつまずいたところ

- **PAR5 は長い＝hard 難易度なので芝が狭い。** 「PAR5 で芝幅3.6m以上」を条件にしたら
  1500シードで0本だった。PAR5 は広い順に選ぶしかない
- **砲台の高さの天井は 0.167m。** 花道の長さ上限5mから
  `高さ = 勾配0.05 × 5m ÷ 1.5` で決まる。0.18 を要求していて0本になった
- 27ホールすべてシードが重複せず、BEGINNER・週替わり候補384シード・
  本番のv1 3コースとも重複しない

### 仕立て（`CONFIG.course.tourSetups`）

つまみは6つ。**新しい3つは既定オフで、オフなら出力が1ビットも変わらない**
（乱数の引き方も引く回数も変えない作りにしてある）。

| | うねり | 速さ | 曲率半径 | S字 | 岸なし池 | 砂の幅 |
|---|---:|---:|---:|---|---:|---|
| BEGINNER | 0.8 | 10ft | ×1 | off | 0 | off |
| STANDARD / ADVANCED / EXPERT | 1.0 | 10ft | ×1 | off | 0 | off |

**3コースはこの表では区別できない。** コース単位では仕掛けをすべてオフにしてあり
（`tourSetups.composed`）、ホールごとに `CONFIG.course.holeFeatures` の部品を足す
（`TourDefinition.holes`）。コース単位で効かせているのは `teeStraightRun` 0.35 だけ。

**グリーンの速さは4コースとも10ft（従来値）。** 一度 8〜12ft に振ったが、
遅いグリーンは1打で進む距離が縮むぶん入門コースがかえって難しくなり、
実機で「重い」と判断された。速さは後から解放する高難度コース専用の要素として取っておく
（`docs/PLAYTEST_BACKLOG.md` §13）。

**実機で見てほしいところ**: 名前どおりの難易度順になったか、
15打のような「止まらないカップ周り」が消えたか、バンカーが同じに見えなくなったか、
EXPERT H2 のS字と H5 の角が「回り込む」判断になっているか、
岸なしの池（フェアウェイが直接水に接する）が理不尽でないか。
**4コースとも同じ絵**なのは変わっていない（`docs/PLAYTEST_BACKLOG.md` §12）。

- **T10 トップメニューと通常ツアーのコース選択: 実装済み・実機確認OK**（PR #56）
  - クエリ指定なしの `/putt/` はトップメニューを表示する
  - 通常ツアーはコース一覧から選択できる（**現在は上の4コース**。当時は v1 の3コース）
  - 保存済み進行があれば `HOLE nから再開` を表示する
  - 練習は既存の練習モードへ接続済み
  - 週替わりチャレンジは現在「準備中」の無効ボタンだが、AdSense審査掲載前に公開トップから一旦隠す予定
- **T10.1 プレイ中と終了後のメニュー動線: 実装済み・実機確認OK**（PR #57）
  - プレイ中は右上の「トップ」から確認画面を経て戻れる
  - 確認中は物理・カメラ・ゲーム進行を停止する
  - 通常ツアー途中は現在ホールの途中経過を保存せず、次回はそのホールの頭から再開する
  - 通常ツアー終了は「コース選択へ / もう一度 / トップへ」
  - 練習カップイン後は「もう一度 / トップへ」
- **通常ツアーの自己ベスト保存: 実装済み・実機確認OK**（PR #64）
  - 9ホール完走時のベストをコース別に `localStorage` へ保存する
  - コース選択画面とラウンド終了画面に `BEST` を表示し、更新時は `NEW BEST!` を表示する
  - 固定ツアーのシード列が変わった場合は旧ベストを無効化する
  - オンラインランキングは**公開済み**（`docs/ranking.md` §10）
- **日本語／英語の二言語対応: 実装済み・実機確認OK**（T10.3）
  - 方針は Multicolor Sweeper に合わせた。文言は `src/i18n.ts` の1オブジェクトに集約
  - 言語は `?lang=ja|en` → `localStorage`（`putt-language`）→ `navigator.languages` → `en`
  - 切り替えはトップメニューとコース選択の下部。ゲーム画面には置かない
  - 遊び方・プライバシーはハブの言語に合わせる。英語表示では `/en/` 配下へ飛ぶ（URLの組み立ては `entry.ts` の `hubUrl()` だけ）
  - コース名は `TourDefinition` に言語別で持つ。`id` とシードは変えていないので自己ベストと進行の保存はそのまま使える
  - ドット絵フォントは言語別のサブセットに分けた。英語表示は 5,456 bytes だけ読み込む（日本語は 63,968 bytes）
  - 英語の文言はゴルフの言い方に揃えた（`HOLED IN 3` / `PAST` / `Whiff` / `BACK TO THE READ` など、PR #69）
  - ギブアップの語はボタンとスコアカードで統一する。初めて遊ぶ人に伝わる `GIVE UP` / `ギブアップ` を使い、`PICK UP` は使わない
  - 日本語も同じ考えで整理した（`長押しでギブアップ` / `マップ` / `3 打でカップイン` など）
  - 俯瞰画面の呼び名は「マップ」/ `MAP` に統一した（ボタン・通知・ソースのコメント）
- **T9 固定ツアーは試遊版3コース・全27ホールを実装済み。最終確定はまだ**
  - T9aでシード1〜1000を走査し、候補表を作成済み（PR #51）
  - T9bで「風の丘」「曲がりの森」「水鏡の庭」を仮選出済み
  - 曲がりの森H8、水鏡の庭H1/H4/H7/H8は面白い例として記録
  - ドッグレッグが直角に寄りやすい、S字が人工的という生成器側の課題も確認
  - 固定ホールの差し替えと生成器v2は **AdSense審査開始のブロッカーにはしない**
  - 詳細は `docs/PLAYTEST_BACKLOG.md`
- **コース生成器v2: 実装済み・実機確認OK（生成器として完成）**（`docs/course-generator-v2.md`）
  - **4回の実機確認を経て採用が決まった**（2026-09-15）。調整の履歴は `docs/PLAYTEST_BACKLOG.md` §1
  - ~~まだツアーには繋がっていない~~ **通常ツアーの4コースはすべてv2で組んだ**（上記）
  - **v1（`src/course/course-generate.ts`）は一切触っていない。**
    シード1〜200でサーフェスのダイジェストとハイトマップの最小・最大高を突き合わせ、
    **v1のコースが1mmも変わっていないことを確認済み**（既存3ツアー・週替わりv1・自己ベストはそのまま）
  - v2は `src/course/course-generate-v2.ts` に新規追加。調整値は `CONFIG.course.generatorV2`
  - **ルートを曲率から作る。** 曲率を弧長に沿った台形の山として置いて積分し、
    その骨格から制御点を2〜4点取って Catmull-Rom で引き直し、1m間隔に刻む。
    器は今までどおりの折れ線で、点数だけが 3.3点 → 19.9点（平均）に増えた
  - **「どれだけ曲がるか」と「どれだけ急に曲がるか」を別々の数字にした。**
    総回頭角 `totalTurn` / 最小曲率半径の下限 `minTurnRadius`（＝芝幅×2）/ 配分区間 `turnSpread`。
    v1は `cornerAngle` ひとつが両方を決め、しかも難易度に直結していた
  - **全長が先、総回頭角が後。** 曲がるのに要る弧長は「半径×回頭角」なので、
    短いホールは大きく曲がれない。**鋭くするのではなく曲がる量のほうを削る**
  - 高さのハザード（マウンド・リッジ・窪地）をハイトマップへ追加。
    新サーフェスも罰打も無し。勾配の上限は `generatorV2.height.maxGradient`（5%）
  - **バンカー（砂）を追加。** `SurfaceType` に `bunker` を足した（波及9ファイル）。
    罰打なし・摩擦はセカンドカットの8倍。
    **水と違って越えられるので、ルートの線の上に置ける唯一の罰則的ハザード**
  - すり鉢は**砂の輪郭そのものを縁にした皿**。輪郭までの正規化距離 q で `1 - q^n` に窪ませるので、
    ひょうたん型でも砂の面全体が窪み、縁でちょうど高さ0に戻って外の芝に斜面が出ない。
    深さはバンカーごとに 5〜26cm でばらつく
  - **距離と par の境目を一緒に伸ばして、パーに対して約3打ぶん厳しくした**（9ホール 163m → 191m）。
    par の境目だけでは境目付近しか変わらない。ツアーは PAR36 固定なので、
    同じ par のホールが長くなることが「厳しい」に直結する
  - 1回目の実機確認で、色はOK・それ以外は調整（`docs/PLAYTEST_BACKLOG.md` §1 の表）。
    ホールの後半だけに置く / 面積を45%減 / フェアウェイへの食い込みを最大0.46mへ /
    ゆるいすり鉢状に窪ませる / ひょうたん・細長い輪郭を低確率で混ぜる（**池も同様**）
  - 高さのハザードは「実感がない」との指摘どおり弱すぎたので、勾配の上限を5%→7%へ上げ、
    尾根をルート上へ寄せた。高低差は最大14.2cm → 19.7cm（うねりの±20cmと同等）
  - **深いラフの島は外した（実機で不採用）。** 詳細は下の「試した結果いらなかったもの」
  - `?gen=v2&seed=N` で1ホールずつ確認できる（`?gen=v2` だけならツアーのシード列をv2で回る）
  - 走査は `npm run survey:v2`（`--gen=v1` で同じ物差しをv1にも当てられる）、
    詰み検証は `npm run check:stuck -- --gen=v2`
  - ~~まだツアーには組んでいない~~ **4コース分を選定してツアーへ組み込み済み**（上記）
  - **数字は下の「生成器v2の実測」を参照。面白いかどうかは実機で見てもらう**
- **風の丘のシードを選び直した: 実装済み・実機確認OK**
  - 試遊で出た「H5/H6/H7が似ている」は、9ホール中7本がストレート・池ゼロ・
    隣り合う地形の重複が3組という**コース全体の単調さ**として数字にも出ていた
  - 生成器は変えず、シード3本の差し替え（798・354・977 → 924・728・423）と
    並べ替えだけで直した。新しい並びは `[553, 924, 232, 468, 848, 728, 44, 423, 185]`
  - 隣り合うホールで形も地形も重複せず、直線とカーブが1ホールおきに入れ替わる。
    曲がりは最大82°（他2コースより緩い）、池は3ホールに1つ
  - PAR3×2・PAR4×5・PAR5×2、PAR4は全て「むずかしい」で5地形を1本ずつ、という条件は維持
  - **風の丘の保存済みラウンド進行と自己ベストは破棄される**（シード列照合。仕様どおり）
  - 週替わり候補との重複なし・詰みなしを `validate:challenge` と `check:stuck` で確認済み
  - **実機で「だいぶ良くなった」と確認できたので、この並びで確定する**（2026-09-14）
  - 採らなかった案（案B・案C）は `docs/PLAYTEST_BACKLOG.md` §1 に理由付きで残した
  - ブランチのプレビュー（`Preview on Cloudflare Workers`）で確認した。本番は未反映
- **オンラインランキング: 登録と取得まで実装済み**（`docs/ranking.md` §10）
  - **登録の動線**: 自己ベスト更新のときだけ、確認を挟まず自動で送る。聞くのは初回の名前だけ。
    失敗・オフラインは「あとで登録します」で保留へ積み、次の起動で送り直す
  - **閲覧の動線**: トップ →「ランキング」の**1枚で完結**。コースはタブで切り替え、
    表の上の「あなた」の行に自分の名前・順位・打数を常に出す。
    ラウンド終了カードからも直接入れる（ボタンが4つになった）
  - 同打数はゴルフ流に `T3` でまとめ、**画面では並べ替えない**。ギブアップを含む記録は `*`
  - **板ID（ランキングの単位）はコースの作り方の指紋で決まる**（`tourFingerprint`）。
    シード列だけでなく生成器・仕立て・ホールごとの上書きも入るので、
    **ホールの仕掛けを1つ足しただけでも板が分かれる**（古い記録が混ざらない）
  - **取得先の既定は `api`（公開済み）。** 止めたくなったら `off` へ戻す。
    入口が消えるだけで**記録は消えない**（`api` に戻せばそのまま出る）。
    画面だけ確かめたいときは `?ranking=mock`（言語と同じで以後も続く）。
    `?rankingMock=empty|crowded|fail-submit|fail-fetch|checking` で分岐を意図的に出せる
  - ホールの規則（罰打・打ち直し・ホールアウト・ギブアップの線）は `src/hole-sim.ts` へ。
    **挙動は変えていない。** 後のリプレイ検証と同じ規則を見るための切り出し
  - サーバは `/api/health`・`/api/rankings`・`/api/records`・`/api/player`（PUT / DELETE）
  - **リプレイ検証まで入った**（`scripts/verify-records.ts` を6時間ごとのワークフローで回す）。
    登録は `pending` で即座に板へ載り、後から `verified` / `flagged` が付く
  - **機械の判定だけで記録を板から外さない**（2026-09-19）。合わなかったものは `flagged` で
    **板に載ったまま**にし、検証ワークフローをわざと失敗させて知らせる（GitHubがメールを送る）。
    板から外す `suspicious` を書くのは人だけ。手順は `DEPLOY.md`
  - **罰打・物理を変えるときは `CONFIG.game.ranking.rulesVersion` を上げる。**
    上げれば板が分かれて古い記録はそのまま残る。上げ忘れても記録は消えず `flagged` が並ぶだけ。
    罰打の規則は `src/hole-sim.ts` の `penaltyStrokes()` 1か所（ゲームと検証が同じ関数を見る）
  - ホールの組み立ては `src/course/hole-build.ts` に1本化した。
    **ゲーム・詰み検証・リプレイ検証が同じ関数を通る**（切り出し前後で36ホール一致を確認）
  - 打ち出しの列（初速と方向）をホールごとに記録して送っている。段5の再生に使う
  - **D1は作成済み**（`putt-ranking`・APAC・2026-09-19）。`wrangler.jsonc` に繋いだ。
    スキーマの適用は Actions の `Apply D1 migrations` を手で実行（`DEPLOY.md`）。
    `0001` と `0002_flagged_status.sql` は**本番適用済み**（2026-09-20）
  - **プライバシーの文言はゲーム側だけ実装済み**（ランキング画面の消し方のすぐ上に一行）。
    **ハブ（`hanage.app/privacy/` と `/en`）の差し替えが残り。**
    貼る文面は `docs/ranking.md` §10-7 に日英とも下書きがある
  - **公開した（2026-09-20）**。`CONFIG.game.ranking.source` を `api` にして main へ。
    本番のD1に移行を流したあとに開けた。公開直後の板は4コースとも空から始まっている
  - **実機確認の結果（2026-09-17）**: モックで一巡し、**ラウンド終了からの登録まで問題なし**。
    1回目の指摘（板の一覧が遠い・言語トグルとコースの説明が不要・自分の順位が分からない）は
    タブ化と「あなた」の行で直し、2回目で確認できた
  - ボタンが4つになったラウンド終了カードは、**枠の中のスクロールで最後まで見られた**。
    「ランキング」を一覧の上へ置く案は採らない
  - **コースを直すと板が分かれて、古い記録は古い板に取り残される。**
    板IDはコースの作り方の指紋なので、**固定コースにはもう手を入れない**。
    コースを**増やす**ぶんには既存の板に影響しない（ただしタブは5つで1行に収まらなくなる）
- **週替わりチャレンジのコアは実装済み**
  - 日本時間の月曜0時を境界に、週キーから全ユーザー共通のPAR3・4・5を1本ずつ選ぶ
  - 生成方式はv1として固定し、同じ週は端末・実行時刻によらず同じ3シードになる
  - v1候補384シードは現在の固定3コース全27ホールと重複しない
  - `Round` / `RoundProgressStore` を使う入口まで実装済み
  - メニュー・画面遷移・ランキングへの接続は、AdSense審査掲載準備より後へ送る
- **DOM側の見た目をプレイ画面のドット感へ寄せた: 実装済み・実機確認OK**
  - メニュー・HUD・スコアカード・確認ダイアログの角丸とぼかし影をやめ、
    太い枠・段差の影・コースの色（fairway / rough / OB / 旗 / 軌跡）で統一した
  - 文字はドット絵フォント DotGothic16（SIL OFL 1.1）。画面に出る字だけのサブセット
    約63KB（4本）を `src/fonts/` に置いて自分たちで配る。実行時に外部へは取りに行かない
  - 文言やコース名を変えたら `npm run font:pixel` でサブセットを作り直す
  - HUDは背景に暗い帯を敷いた（空の上でドットの細い線が読めないため）。
    スコアカード表示中は帯ごと引っ込める
  - **実機で「HUDと下部ボタンがプレイ画面を邪魔する」と分かったので両方詰めた**
    - HUDは1行だけにした: ホール・PAR・打数・スコア・カップまでの距離。
      視点名・方角・直前の結果・スワイプの数値は `?debug=1` のときだけ出す
    - 池／OBの知らせは残すが、空のときは行ごと畳んで帯を伸ばさない
    - 視点バーは4つを1行に戻した。3Dの上に重なるボタンだけ12pxへ落とし、
      押す高さ（36px）は保つ。カードとダイアログの中は16pxのまま
    - HUDの帯は高さを固定した（知らせが無いときも1行ぶん空ける・55px）。
      練習の「強さ」ボタンは帯の外へ出した
    - 3Dの上に重なるボタンは全部 36px / 12px、カードとダイアログの中は 44px / 16px に揃えた
  - コース選択は枠を押させず、枠の中のボタンだけを押させる。
    途中保存があるときは「最初から」と「HOLE nから再開」を横に並べ、
    無ければ「最初から」だけを出す（保存があるときの最初からは、始める前に保存を消す）
  - ギブアップは「ギブアップ 長押し」と書き、押している間は「押したまま…」に変わる
  - コース選択が縦に収まらないと上が切れていたのを直した（中央寄せ → margin auto）
  - 画面構成・遷移・文言・当たり判定の位置以外の挙動は変えていない
  - PWAの素地（マニフェスト・アイコン・headタグ）を取り込んだうえで、
    `npm run build` と `npm run build:cloudflare` の両方が通ることを確認した
  - **実機で確認した点**: HUDの帯の高さ（情報量が多いホールで画面をどれだけ食うか）、
    視点バーとマップ／ギブアップの押しやすさ、
    コース選択の「最初から」と「再開」を押し間違えないか

- **パターヘッドの見た目と形状選択: 実装済み・実機確認OK**
  - STROKE のパターをパターと分かるシルエットにした（胴・後ろの張り出し・ホーゼル・
    シャフト・照準線）。芯の範囲はフェース面のインサートとして暗く塗り、
    打てるかどうかの状態色はトウ・ヒール側のフェースと照準線に出す
  - 4形状から選べるようにした: ピン型（既定）/ L字（真鍮の金色）/
    マレット（かまぼこ）/ ネオマレット（二股）
  - **見た目だけの選択で性能差は付けない。** フェース長・芯の範囲・打ち出しの計算は共通で、
    芯の印もどの形状でも同じ大きさ・同じ描き方（マレットが有利に見える錯覚を消すため）。
    ランキングの公平性を壊さないための約束なので、後から性能差を付けない
  - トップメニューの「練習」の下に置いた「パターを選ぶ」から選ぶ。遊び始めるボタンでは
    ないので色を落として区別する（3つとも同じ幅で縦に並べる）。
    見本・名前・「選ぶ」の1行を4つ並べるだけで、
    形の説明も注意書きも書かない。文言は二言語対応（`src/i18n.ts` の `putterShapes` ほか）。`putt-putter-shape` に保存し、読めない・知らない値なら既定へ落とす。
    選択画面の見本はゲーム本体と同じ描画関数で描く
  - 形状データは `CONFIG.game.stroke.putterShapes`、描画は `src/putter-shape.ts`
  - `/swipe-test/` は計測の検証ページなので長方形のまま残した
  - **実機で確認した点**: 待機中のパターが芝の上で読めるか（特に細いL字）、
    フェース面の位置が今までどおりボールのすぐ後ろに見えるか、振っている最中に向きが
    分かりやすいか、トップで「パターを選ぶ」を「練習」と押し間違えないか

- **ラウンド終了カードの見切れ対策: 実装済み・実機確認OK**
  - 9ホールの一覧を出すカードは高さ674pxあり、画面の高さが約610px以下（iPhone SE や
    ツールバーが出ている Safari）だと上下が切れていた
  - 一覧表を出すカードだけ字と余白を詰めて575pxにし、それでも収まらない画面では
    枠は画面に収めたまま**中身だけ**を縦にスクロールできるようにした（`#score-card.list`）
  - 指を受けるのは枠の中だけなので、「トップ」ボタンは今までどおり押せる（重ね順は変えていない）。
    カードは「トップ」ボタンの行より下から置く。ホール間のカードは今までどおり指を通し、
    画面のどこをタップしても次へ進む
  - **実機で確認した点**: 9ホール終了時にカードの上下が切れないか、
    切れる画面で指でスクロールできるか、ホール間のカードがタップで進むか

- **パター選択画面のスクロール解消: 実装済み・実機確認OK**
  - 見本の枠を 56×84 → 56×60px にし（シャフトは根元だけ入れて残りは枠の外）、
    余白を詰めてパネルを666px → 503pxにした
  - 320×568 まで**スクロールなしで4本が収まる**。「← トップ」が画面から消えない

- **練習モードの「コース変更」: 実装済み・実機確認OK**
  - ADDRESS のときだけ右下（マップの上）に出し、押すとランダムなシードで
    地形・外形・カップを引き直す。引いたシードは `?seed=` に書き戻す
  - 練習モードだけ。ツアーは固定9ホールを順に回るのでホールを差し替えない。
    マップを開いている間は重ねない
  - シードの範囲は `CONFIG.game.practice.seedRange`
  - **実機で確認した点**: マップと縦に並べて押し間違えないか、
    ギブアップが出ている場面（ダブルパー）で画面が窮屈にならないか

- **ゲーム本体の基盤は実機確認済み**
  - T0〜T3、T3.5〜T3.8、T4、T7、T7.1、T7.2、T8、T8.1、T8.2 は主要な実機確認まで完了
  - 長方形コース、生成器、コースマップ、ラウンド進行、保存・再開、カップ物理、ギブアップまで動作している
  - 開発用表示は `?debug=1` のときだけ露出する

- **Cloudflare Workers へ切り替え済み（2026-09-06）**
  - 本番は `https://putt.hanage.app/`。実機（iPhone）でトップメニューから
    通常ツアー・遊び方・プライバシーまで確認済み
  - GitHub Pages（`https://hanagemaru.github.io/putt/`）は退避先として当面併走する。
    `main` への push で両方に配信される
  - hanage-hub の `GAME_URLS.putt` も新URLへ差し替え済み
  - 設定値・ロールバック・残作業（Pages の停止）は `DEPLOY.md`

- **サウンド: 実装済み・実機確認OK**（2026-09-11〜09-12、PR #86 ほか）
  - 効果音は実録ベース（`src/audio-assets/`）。打音・カップイン・旗竿・池・OB・メニュー操作音
  - BGMは Web Audio で組み立てる（`src/music.ts`）。トップ・プレイ中・ラウンド終了で
    同じ世界観の別アレンジに分ける。ホールアウトの音はスコアカードと同時に鳴らす（下記）
  - 音の入り切りはトップメニュー下部の「音 ON/OFF」。`putt-sound-enabled` に保存する
  - バックグラウンド復帰で音が戻らない問題は修正済み（PR #87、`src/audio-context.ts`）
  - **音量・遅延などの数値が `src/config.ts` に無く、各モジュールの定数になっている**
  - `docs/spec.md` §7 は「サウンド（現段階）」を実装しないことに挙げたままで、現状と食い違う
  - 試遊で出た「ホール間のジングルが浮く」は修正して実機確認OK（`docs/PLAYTEST_BACKLOG.md` §9）

- **PWA: 実装済み・実機確認待ち**
  - マニフェスト（`standalone` / `portrait`）とドット絵アイコン（192/512・マスカブル・apple 180）
    は実機確認OK。`start_url` / `scope` / アイコンのパスは配信先のベースに追従する
  - Service Worker（`vite-plugin-pwa`）を追加。一度開けばオフラインで遊べる。
    新しい版は待機させ、次の起動で入れ替わる。**実機確認はまだ**（構成は `docs/pwa.md`）

- **OB・池の画面通知: 実装済み・実機確認OK**（PR #89 / #91 / #93）
  - 池・OBに入ったRESULT中、既存の知らせ欄へ `池（1罰打）・n 打` / `OB（1罰打）・n 打` を表示する
  - OBは俯瞰カメラの遷移完了と同時に画面中央へ白で大きく表示する。日本語は `OB`、英語は `OUT OF BOUNDS`
  - 次の一打へ進むと通常の操作案内へ戻る。物理・罰打計算は変更していない
  - 本番 `https://putt.hanage.app/` で実機確認済み

- **PCのギブアップ長押し: 修正済み・実機確認OK**（PR #94）
  - 長押し中のポインターをボタンへ明示的に捕捉し、押下中の見た目移動などでタイマーが解除されるのを防ぐ
  - ボタン外へ実際に動かした場合のキャンセル、長押し時間、スマホ操作は変更していない

- **カップイン後の待ち: 修正済み・実機確認OK**
  - スコアカードの待ち（`round.cardDelay`）を、俯瞰カメラの遷移が終わってから数え始めるようにした
  - これまでは遷移の途中（停止の約1.15秒後）にカードが重なり、最後の一打のラインを見られなかった
  - カードが出るのは停止から約2.95秒後になる。待ちきれないときのタップでの早出しは従来どおり
  - 長さの調整は `src/config.ts` の `round.cardDelay` だけでできる。物理・罰打計算は変更していない

- **進行表示（何ホール目・何打目）: 修正済み・実機確認OK**
  - 中継のホール表示を左端に置き、その右へ3つの読み取りを同じ大きさで並べた。
    `[2][20m / PAR 4]  SHOT 1  TOTAL +3  TO PIN 20.31m`
  - ホール表示は白地に反転した番号＋全長／PARの2段。**2段でも1行ぶんに収め、帯は55pxのまま**。
    全長は芝の中心線（`route`）に沿った長さ。実際のゴルフのヤード表記と同じで
    **ティーからの残り距離とは一致しない**ため、整数で出して桁を揃えない。ホール紹介も同じ数字
  - 打数は「打った数」ではなく「今どの一打か」を出す（中継の「第2打」と同じ数え方）
  - 通算パー差は `TOTAL` の語を付けて取り違えを防ぐ。アンダーは赤、オーバーは青（記号も残す）
  - 残り距離は `TO PIN` を付ける（中継と同じ言い方）。転がっている間も出し続ける
  - **帯の中の語は日本語でも英語のまま**（`SHOT` / `TOTAL` / `TO PIN` / `PAR`）。
    パー差のイーブンは `E`（カードの中は `±0` のまま）
  - ホールの入り口に `HOLE 3` / `PAR 4・12m` を2秒だけ**画面の中ほどへ**大きく出す。ツアーのみ。
    出るときはカメラの切り替わりと同時にフェードなし、消えるときだけフェード（0.25秒）。
    時間は `src/config.ts` の `round.holeIntro`

- **練習のカードの「練習終了」の文言: 削除済み・実機確認OK**
  - カード自体は残し、見出しを `PAR 4` だけにした。カードの構成と操作は変えていない

- **画面端スワイプでブラウザが戻る問題: 修正済み・実機確認OK**
  - iOS Safari の端スワイプは `touch-action: none` では止まらず、`touchstart` を
    打ち消したときだけ止まる。左右端から始まった指だけ打ち消すようにした
  - 止める幅は `src/config.ts` の `game.edgeSwipeGuard`（28px）

- **ホール終了・ラウンド終了のカード: 修正済み・実機確認OK**
  - HUDと同じ組み方へ揃えた。**中継と同じで、スコアはまず語で言う**
    （`DOUBLE BOGEY` を主役に、その下へ `6 STROKES`、さらに下へ `TOTAL 19 +1`）
  - PARはそのホールの素性なので、1行目のホール番号の隣へ移した
  - ラウンド終了は判定語が無いので、中継のリーダーボードと同じく通算パー差を主役にする
  - パー差の書き方をゲーム中で1つにした。イーブンは `±0` → `E`（英語のゴルフ表記）。
    スコア表・自己ベストの表示も同じ書き方になる
  - `TOTAL` はパー差、打数の合計は `STROKES`（英語のリーダーボードの語の当て方）。
    2つは別の欄に分けて離す
  - あわせて、自己ベストの箱が `hidden` で消えない不具合を直した。
    空の黄色い箱がカードに残り、一度出したベストがホール間のカードにも出続けていた

- **ホールアウトのジングル: 修正済み・実機確認OK**
  - 試遊で「浮いて聞こえる。もっと静かに」と出た。原因はBGMとジングルという
    **別々の曲が同時に鳴っていた**ことなので、重なり自体をなくした
  - **スコアカードが出るのと同時**に、BGMを0.35秒で引いてから結果別のジングルを鳴らす。
    カードを見ている間は無音。**次のホールが始まる（カードが消える）**とBGMが0.7秒で戻る
  - 結果は5段階（イーグル以上 / バーディ / パー / ボギー / ダボ以上）。
    5つとも同じ2〜4音の形で、上行か下行か、♭が入るかだけが違う
  - 音色はBGMと同じ（三角波のベース + 単音のPSG）。同時和音とノイズヒットは使わない。
    BGMを止めてから鳴らすので、音量は 0.28 → 0.22 で足りる
  - BGMは止めずに音量だけ引いて曲は裏で進ませる。戻したとき毎回同じ出だしにならない
  - **ギブアップでは鳴らさない。** カップインしていないので追い打ちにしない
  - 繋ぎ方は `src/main.ts`（カードの結果を `data-result` に書く）と
    `src/audio-bootstrap.ts`（それを見て鳴らす）。main.ts は音を直接呼ばない
  - **実機確認OK。** 採用したので、比較用に置いていた6案（`JingleVariant` /
    `playHoleOutJingle` / `ACTIVE_JINGLE`）と試聴ページのB欄は削除した
  - 試聴ページ `/jingle-test/` は、結果別の5つを本編と同じ流れで確かめる場として残す

## 試した結果いらなかったもの: 深いラフの島（2026-09-15・実機で不採用）

セカンドカットを帯ではなく塊としてルートの中や脇に置き、「近道側だけ重い」を作る案
（`docs/course-generator-v2.md` §2-3 のハザード4）。**実機で「ただのラフが真ん中に
あるのと同じで、ただ邪魔なだけ」と判断されたので、コードごと削除した。**

後から数えたら、実装が意図どおりになっていなかった。

- 島294個のうち **87% はルートの中心線が塊の中を通っていた**
- **100% が芝の内側半分を塞いでいた**
- 原因は横ずれの下限が 0 だったこと。中心に置ける以上、逃げ道が残らない

逃げ道が無ければ「近道を切るか回るか」の選択にならず、ただ避けられない重い地面になる。
**バンカーではこれを踏まえて `minClearWidth` で必ず反対側に通れる芝を残すようにした。**
実測で、砂が中心線に掛かっている1048断面すべてで脇に1.8m以上の芝が残っている。

## 生成器v2の実測（シード1〜1000・`npm run survey:v2`）

同じ物差しをv1にも当てて並べたもの。**機械で見る合格条件（`docs/course-generator-v2.md` §5）は全て通った。**

| | v1 | v2 |
|---|---:|---:|
| fallback | 0本 | **0本** |
| ティー側から到達できない芝の島 | 16本（1.6%） | **0本** |
| OB面積比が18〜45%の外 | 0本 | **0本** |
| 最小曲率半径が下限未満 | （下限なし） | **0本** |
| 総回頭角 中央値 | 65.8° | 38.1° |
| 総回頭角 最大 | 202.1° | 95.3° |
| 総回頭角が90°以上 | 25.9% | **0.1%** |
| 最小曲率半径 中央値 / 最小 | 8.37m / 3.30m | 10.86m / 6.99m |
| 遠回り率 中央値 / 最大 | 1.12 / 1.64 | 1.02 / 1.20 |
| むずかしいPAR4のルート全長 | 20.0〜22.0m（幅2.0m） | **17.0〜22.0m（幅5.0m）** |
| ルートの点数（平均） | 3.3点 | 19.9点 |

詰み検証（`npm run check:stuck --seeds=1-40`・v1とv2を同じ範囲で。1000本は時間が足りない）。

| | v1 | v2 |
|---|---:|---:|
| 詰み（どう打っても池かOB）のマス | 0 | **0** |
| ティーとカップが繋がっていないシード | 0 | **0** |
| ティー成分の外の芝の島 | 0 | **0** |
| カップから1m以内で入れられなかったマス | 0 / 492 | **0 / 486** |
| 池・OBを跨いで止まったショット | 0 | **0** |
| 60秒で止まらなかったショット | 0 | **0** |
| 「カップへ行けない」マス | 0 | **0** |

**バンカーを入れた後も、実機の指摘で2度調整した後も同じ結果だった**（54.4万ショット・5.0万マス）。

すり鉢を輪郭に沿わせ、深さをばらつかせ、距離を伸ばした後の数字（56.9万ショット・5.6万マス）。
**いちばん急な「摩擦が重い地面」は砂の42.5%**（シード6）で、そこから最強の一打で
9.38m 動いて止まる。40シードで調べた急斜面のうち**いちばん動かせなかった地点でも2.26m**
動く。すり鉢の形を「縁がいちばん急・底は平ら」に変えたことで、
**前（中途が急・底も縁も平ら）の1.67mより出やすくなっている。**
砂は「止まれるマス」に数えているので、**「打っても出ない砂」があればここで詰みとして出る。**
止まれる勾配の上限は砂で62.7%、砂の上の最大勾配は実測25.4%なので、砂で転がり続けることもない。
5.0万マスすべてが最弱の一打（0.15m/s）で動かせた。

調べたのは v1 53.3万ショット・4.7万マス / v2 56.4万ショット・5.0万マス。

**「カップへ行けない」マスは、しきい値を直す前は1マス出ていた。**
シード19の、カップの7.4m奥・周りを1mのOBに囲まれたセカンドカットの数マス。
転がって届かないので実害はない（実際どのショットもそこで止まっていない）が、
**v1の同じ40シードでは0だったので、v2だけの症状だった。**
`maxUnreachableArea` を 0.25m2 → 0.01m2（1マス未満）にして、1マスでも残ったら
作り直すようにしたら消えた。作り直しが増えたのは300本に2本、平均試行回数 1.84 → 1.85 回。

読み取れること。

- **v1の症状は数字の上では消えた。** 90°前後へ寄っていた折れ角は 0.1% になり、
  「むずかしいPAR4が20〜22mしか作れない」も 17〜22m に広がった
- 芝の島は0本。生成時の検証を `check:stuck` と同じ 0.15m 格子にし、
  連結（ティー→カップ）だけでなく**到達できない芝の面積**も合格条件に入れたため。
  しきい値は1マス未満なので、**1マスでも到達できない芝があれば作り直す**
- **止まれない面（勾配が摩擦を上回る面）は v1 0.82% → v2 1.46% に増えた。**
  高さのハザードの勾配を7%まで上げたぶん。`check:stuck` で詰み0・カップイン可を確認済み。
  なお**サーフェスごとに止まれる勾配の上限は違う**（通常芝7.8% / ラフ27.4% /
  セカンドカット47.0% / 砂62.7%）。一時ここに「v1 2.18% → v2 1.94%」と書いていたが、
  全部を通常芝の基準で数えた誤りだった
- **遠回り率はv2のほうが小さい**（中央値 1.12 → 1.02）。
  曲率半径に下限を設けた以上これは幾何の帰結で、同じ全長なら曲がれる量が減る。
  **「回り込む」感じが薄くなっていないかは実機で見てもらう必要がある**（`docs/PLAYTEST_BACKLOG.md` §1）

## 記録済みの未確定改善

詳細と優先度は `docs/PLAYTEST_BACKLOG.md` を正とする。

**2026-09-13の試遊で追加した4件（遊んでいる最中の分かりにくさ）: すべて完了**

- OB・池の画面通知は実装・実機確認とも完了（PR #89 / #91 / #93）
- PCのギブアップ長押しは修正済み（PR #94）。**実機確認OK**
- 進行表示は修正済み（打数を主役にし、ホール紹介を追加）。**実機確認OK**
- カップイン直後のカードは修正済み（遷移完了後から数える）。**実機確認OK**
- ホール間のジングルが浮く → 修正済み（カードと同時にBGMを引いて結果別に鳴らす）。**実機確認OK**

**以前から記録しているもの**

- ~~固定ツアーの並び・シード再選定~~ **v2の4コースへ一新して解消**（実機確認待ち）
- 自然なドッグレッグ / S字を作るコース生成器v2
  - 第1段階は実装済み（2026-09-15）。**実機で見比べて採否を決める**
  - 現行生成方式は固定シード・週替わりv1の再現用に残してある（v1の出力は不変を確認済み）
- 本コースとは別の、比較的平坦で人工的な練習グリーン
- ~~パターの「敏感すぎる」感触の切り分けと調整~~ **一旦ペンディング**（2026-09-15）
  - 試遊で問題提起はしたが、今のバランスで良いと判断した。気になったときに再開する
- OB境界の視認性（上のOBの知らせを入れてから改めて判断する）
- ボール走行中のタップで、同じ物理結果のまま結果までスキップする機能

## 審査掲載準備（P0-1〜P0-3・すべて完了）

**Putt側に残っている作業はない。** 経緯として各項目の内容を残す。

### P0-1. プライバシー・遊び方・未完成表示

**完了（実装・実機確認とも済み）。**

- 公開トップは「通常ツアー / 練習」の2モードだけを大きく表示する
- 未接続の「週替わりチャレンジ（準備中）」は公開トップから一旦隠した
- トップ下部の「遊び方」から、コース確認・狙い・構え・ストローク・池/OBの基本操作を確認できる
- トップ下部の「プライバシー」から `https://hanage.app/privacy/` を別タブで開く
- 週替わりチャレンジの内部コアは削除していない
- 通常ツアー、練習、物理、固定ホールは変更していない

### P0-1.5. 二言語対応の実機確認

**完了。** 英語表示で以下を確認した。

1. 視点バー（AIM / LOW / CUP / SIDE）が1段に収まるか
2. HUDの進行1行が日本語より増えていないか（実測では英語のほうが短い）
3. トップメニュー下部の言語トグルが「遊び方 / プライバシー」と混ざって見えないか
4. 端末言語が英語の状態で初回起動したときに英語で出るか、切替後にリロードしても保持されるか
5. ラウンド終了カードの一覧表と見出しが小さい画面でも崩れないか
6. 英語表示の字が、日本語表示と同じドット絵フォントで出ているか（別のフォントに落ちていないか）

### P0-2. OB境界（必要なら）

**着手しない。** P0-1・P0-3の実機確認で改善要求が出なかったため、審査掲載のブロッカーとしない。
今後の試遊で通常プレイの判断を妨げると分かった場合に、白杭などの境界マーカー、または
セカンドカットとOBの見た目差を候補として最小限の変更で対応する。
視認性の論点は `docs/PLAYTEST_BACKLOG.md` に残っている。

### P0-3. 審査掲載確認

**完了。** 以下を実機で一巡した。

- 公開入口 → 通常ツアー → 9ホール終了 → 戻る
- 練習入口とトップ復帰
- プライバシーと遊び方のリンク（日本語・英語とも。英語は `/en/` 配下へ飛ぶ）
- 開発用UI・リンク切れ・空画面がないこと
- hanage.appからPuttへ到達できること

**ここまででPutt側はAdSense審査へ載せる状態になった。**

## 次にやること

**審査掲載準備は終わっている。Putt側の次の作業はここから1件ずつ選ぶ（1セッション1タスク）。**

「遊んでいる最中に分かりにくい」ものは直し終えた。ここからはコースの中身と機能追加。

1. ~~**OB・池に入ったことを画面で知らせる**~~ 完了（同 §5-2。PR #89 / #91 / #93、実機確認OK）
2. ~~**PCでギブアップの長押しが成立しない問題を直す**~~ 完了（同 §10。PR #94、実機確認OK）
3. ~~**カップイン後にラインを見る間を作る**~~ 完了（同 §8。実機確認OK）
4. ~~**進行表示を読み取りやすくする**~~ 完了（同 §7。実機確認OK）
5. ~~**ホールアウトのジングルを落ち着かせる**~~ 完了（同 §9。実機確認OK）
6. ~~風の丘H5〜H7を重点に、固定ツアーの差し替え・並べ替え~~ 完了（実機確認OK）
7. ~~**コース生成器v2**~~ **生成器としては完成**（2026-09-15・実機確認OK。正本は `docs/course-generator-v2.md`）
   - ルートの曲線化・高さのハザード・バンカー・`?gen=v2`・走査まで入った
   - 深いラフの島は実機で不採用になり削除した
   - ~~**7-1. v2をツアーへ繋ぐ**~~ **完了（実機確認待ち）**
     - `TourDefinition` に `generator` と `setup` を足した（省略時v1・既定の仕立て）
     - v2で4コース分のシードを選び、**v1の3コースは一覧から外した**
     - 週替わりチャレンジは当面v1のまま（`CHALLENGE_SEED_POOLS_V1` は触っていない）
   - 7-2. クリーク（細長い水）。既存の水の仕組みに乗るので `EllipseHazard` を union にするだけ
   - 7-3. 芝幅をルートに沿って変える / カップをルート終端からずらす（`CourseDefinition` の拡張が要る）
   - ~~7-4. 曲がりの森・水鏡の庭の選び直し~~ **v2の4コースへ一新して解消**
7-5. ~~**コースごとの雰囲気を出す**（`docs/PLAYTEST_BACKLOG.md` §12）~~
   **実装済み・実機確認待ち。** 空・光の強さ・地面の色・木をコース別に持たせた
   （`CONFIG.themes`）。8-2（木）と一緒に入れた
7-6. **高難度コースの解放**（同 §13）。グリーンの速さ（12ftなど）はここで使う。
   4コース完走で解放、が第一候補。ランキングを分けるかを一緒に決める
8. ~~パター感度の切り分け・調整~~ **一旦ペンディング**（今のバランスで良いと判断。同 §3）
8-1. ~~**OB境界の視認性**（同 §5-1）~~ **確定・マージ済み**（線でなぞる形に決まった）。
   以下は着手前のメモ。**生成器とは無関係**なのでいつでも着手できる。
   ただし境界の「形」（`CONFIG.course.edgeNoise`）は**v1とv2で共有**しているので触らない。
   触ると既存3ツアー全27ホールの外形が変わり、自己ベストも無効になる。色・目印で解決する
8-2. ~~**木のバリエーションと細部の作り込み**（同 §11）~~
   **実装済み・実機確認待ち。** 広葉樹・針葉樹・低木の3種にして、1本ごとに乱数で揺らぐようにした。
   触ったのは `createTrees` と `CONFIG.trees` だけ
9. 練習グリーン専用化
10. 転がりスキップ
11. 週替わりチャレンジの画面接続
12. ~~オンラインランキング~~ **公開済み（2026-09-20）**。設計と実装は `docs/ranking.md`
   - 画面と動線・規則の切り出し・サーバ・登録と取得・リプレイ検証・`flagged` の判断まで（同 §10）
   - **Workersは無料枠のまま。** ランキング本体は無料枠で足り、詰まるのは検証だけ
   - **リプレイ検証はGitHub Actionsの定期バッチでNode上で回す**（Workerの10ms CPUを避ける）
   - 残り: ハブのプライバシーポリシー差し替えと、週替わりチャレンジの板（11のあと）
13. 広告を含む完成版のゲーム遷移

**1〜6（試遊で出た「遊んでいる最中の分かりにくさ」と風の丘の並び）は実機確認まで終わった。**
**7-1（v2をツアーへ）・7-5（コースごとの雰囲気）・8-2（木）まで入った。**

**次は 7-6（高難度コースの解放）。** ただしその前に、**実機確認が3件たまっている**
（v2の4コース／旗竿の反発2.1／今回のテーマと木）。まとめて1回で見られる。

## コースを見比べる道具

**`npm run maps`** で、通常ツアー全36ホールを真上から並べた1枚のHTMLを作る
（`docs/course-maps.html`。生成物なので git には入れない）。

- 36ホールすべて**縮尺が同じ**なので、画像の大きさの違いがそのままコースの広さの違いになる
- 濃淡・色はゲーム本体のマップと同じ（明るい＝高い。池とOBは濃淡を掛けない）
- ホールごとに seed・形・地形・芝幅・曲がり・遠回り率・池と砂の数を添える
- 「回り込む」「岸なし池」はタグで目立たせる

**本番（main）にある v1 の3コースも同じ紙に並べる。** 見比べる相手が要るので、
シード列を `LIVE_V1_TOURS` に写してある（本番から v1 が消えたらこの定数ごと消す）。

**シードを選び直したら必ずこれを見る。** 数字（全長・芝幅・遠回り率）だけでは
「同じような形が9本続いている」が分からない。

本番の3コースを並べて分かったこと。

| | 全長 | 平均芝幅 | 平均曲がり | 遠回り最大 | 池 | 砂 |
|---|---:|---:|---:|---:|---:|---:|
| 風の丘（本番） | 178m | 3.87m | 17° | 1.03 | **0個** | 0個 |
| 曲がりの森（本番） | 176m | 3.81m | 99° | **1.64** | 8個 | 0個 |
| 水鏡の庭（本番） | 177m | 3.74m | 97° | 1.52 | 15個 | 0個 |

- **v1の3コースは全長も芝幅もほぼ同じ**（176〜178m / 3.74〜3.87m）。
  違いは曲がりと池だけで、3本とも「長さと広さ」が同じ土俵にある
- **風の丘（本番）は池も砂もゼロ**。曲がりも平均17°しかない。
  ブランチのBEGINNER（152m・芝幅5.17m・砂8個・27°）より**長くて狭い**
- v1は遠回り率が 1.52〜1.64 まで出ている。**v2はまだここに届いていない**（最大1.39）

## 残っている小さな宿題

- **練習モードと週替わりチャレンジは見た目のテーマを持たない**（既定の空・色のまま）。
  木の形だけは全モードで新しくなる。テーマを持たせるかは、通常ツアーの実機確認のあとで決める
- **練習モードと週替わりチャレンジはまだ生成器v1のまま。** 通常ツアー4コースを全部v2にしたので、
  v1で作ったホールはこの2つでしか出なくなった。練習をv2へ寄せるかは別途判断する
  （`?seed=N` の見比べの基準が変わるので、一緒に決める）
- `docs/spec.md` §6 のコース一覧が旧3コース（風の丘・曲がりの森・水鏡の庭）のまま。§6を触るときに直す

- 最終ホールのホールアウトカード表示中に離脱すると、再開できる次ホールがないためラウンドは最初からになる。ROUND_ENDの復元は未実装
- `docs/spec.md` §4に旧「手元を見る」ボタン前提の記述が一部残る。次に§4を触るときに現実装へ揃える
- 生成器が「ティー側から到達できない芝」を作ることがある。シード1〜1000では15本（1.5%。66・107・149を含む）。現在は転がって到達できず無害で、固定ツアー27ホールには1本も入っていない。池配置や `waterFringe` を触るときは検証条件へ追加する
- **実機のスワイプ速度の上下限が未実測。** `speedK` やパター感度を今後調整するときは `/swipe-test/` で実測する（パター感度の調整自体はペンディング）
- サウンドの数値（音量・遅延）が `src/config.ts` に無い。次に音を触るときに寄せるか判断する
- `docs/spec.md` §7 の「サウンド（現段階）を実装しない」が現状と食い違う。§7を触るときに直す
- Cloudflare Web Analytics のPR（[#85](https://github.com/hanagemaru/putt/pull/85)）が未マージのまま。マージ後に `CF_BEACON_TOKEN` の設定が要る
- Issue [#60](https://github.com/hanagemaru/putt/issues/60)（プレイ中のトップ復帰ボタンの見え方）が開いたまま。HUDまわりを触るときに合わせて判断する

## まだ未実装の主なもの

### AdSense審査掲載前に対応

**なし。** P0-1〜P0-3は完了し、OB境界（P0-2）は着手しないと判断した。
申請そのものの手順とハブ側の準備は `hanage-hub` の正本に従う。

### 審査を待たせない

- 試遊用3コースの最終確定
- コース生成器v2
- 専用練習グリーン
- パター感度・転がりスキップ
- 週替わりチャレンジのメニュー・画面遷移・進行保存の接続
- オンラインランキング（`docs/ranking.md`。画面・規則・器は実装済み、段4以降が残り）
- 広告を含む完成版のゲーム遷移

## セッション開始時

1. GitHubの最新 `main` とオープン中のPRを確認する
2. この `PROJECT_STATUS.md` を読む
3. `RELEASE_PLAN.md` で審査掲載準備と初回公開の残工程を確認する
4. 使用するAIの入口（`CLAUDE.md` または `AGENTS.md`）を読む
5. 未確定の試遊改善を扱う場合は `docs/PLAYTEST_BACKLOG.md` を読む
6. 必要な範囲だけ `TASKS.md` と `docs/spec.md` を読む
7. 最新 `main` からタスク専用ブランチを作る

チャットの過去ログや、pushされていないローカル状態だけを引き継ぎ元にしない。

## セッション終了時

- 「現在地」「実機確認待ち」「次にやること」が変わった場合は、このファイルを更新する
- 残工程・依存関係・審査掲載または初回公開の完了条件が変わった場合は `RELEASE_PLAN.md` を更新する
- 未確定の試遊フィードバックは `docs/PLAYTEST_BACKLOG.md` に残す
- ゲーム仕様・UXを変えた場合は `docs/spec.md` を同期する
- タスク構成・履歴・実装順を変えた場合は `TASKS.md` を同期する
- 恒久的なAI向けルールが変わった場合だけ `CLAUDE.md` / `AGENTS.md` を更新する
- ブランチ、PR、確認内容をGitHub上に残し、次のAIが再開できる状態にする

## 集客前のアクセス解析（2026-09-20）

- Cloudflare Web Analyticsの読み込みを本番ドメインに限定し、ローカル・プレビュー・録画/検証モードを除外する変更を準備。広告配信は変更しない。
- 本番ビルドはリポジトリ変数 `CF_BEACON_TOKEN` を `VITE_CF_BEACON_TOKEN` に渡す。Cloudflare側で当該ゲームをManual setupで登録したサイトトークンが必要。値をコードへ書かない。
- マージ・本番配信後、通常のゲームURLでビーコン要求とCloudflare管理画面の集計を確認して計測開始とする。トークン未設定なら読み込まない。
- この変更はページ閲覧の計測のみ。プレイ開始・終了・再挑戦・再訪のイベント計測は未実装で、PVをプレイヤー数や継続率と扱わない。
