# 現在地 / Project Status

最終更新: 2026-09-05（**試遊用3コースを実装中。次は27ホールの実機確認**）

このファイルは **Claude / ChatGPT / Codex 共通の短い引き継ぎ用「現在地」**です。
新しいセッションでは、長い履歴を最初から読み直す前にまずここを確認します。

## ファイルの役割

- `PROJECT_STATUS.md`（このファイル）: 今どこまで終わっているか、実機確認待ち、次にやることを短く保持する
- `RELEASE_PLAN.md`: 初回公開までの工程・依存関係・完了条件を保持する
- `CLAUDE.md`: Claude向けの入口。技術・設計上の恒久ルールを保持する
- `AGENTS.md`: ChatGPT / Codex向けの入口。上記と同じ正本へ誘導する
- `TASKS.md`: 実装済みの履歴と、より長いタスク分割・実装順を保持する
- `docs/spec.md`: ゲーム仕様・UX・数値などの詳細な正本

同じ仕様を複数ファイルへ大量に複製しない。矛盾した場合は、ゲーム仕様は `docs/spec.md`、恒久ルールは `CLAUDE.md`、現在地はこのファイルを優先して確認する。

## 現在地

- **T9a 固定9ホールの候補表を作成済み**（PR #51）
  - シード1〜1000を機械的に走査し、PAR×地形ごとに2本ずつ、計30本へ絞った
  - `docs/tour-hole-candidates.md` のシード番号から実機ページを直接開ける
  - T9bとして試遊用3コース・全27ホールを仮選出済み。人が通して遊び、違和感を確認する段階
- **週替わりチャレンジのコアを実装済み**
  - 日本時間の月曜0時を境界に、週キーから全ユーザー共通のPAR3・4・5を1本ずつ選ぶ
  - 生成方式はv1として固定し、同じ週は端末・実行時刻によらず同じ3シードになる
  - v1候補384シードは固定3コース全27ホールと重複しない。固定ホール変更時も検証で重複を検出する
  - `Round` / `RoundProgressStore` を使う入口まで実装。メニュー・画面遷移・ランキングは未接続
- ゲーム本体の直近の大きな完了は **T8.2「打ち切りの廃止とギブアップ」**（PR #49）
- T0〜T3は完了済み
- T3.5「長方形コース基盤と試験ホール」は **実装済み・実機確認OK**
- 実機確認1回目で「OBになりやすすぎる」と判明し、芝の階層と表示を調整し、再確認OK
- 実機確認2回目で見つかった方向調整画面の表示不具合2件を修正し、確認OK
  - 長いホールでボールが画面外へ落ちる → ボールが画面内に残るピッチを幾何から解く方式にした
  - 打ち出し方向ガイドがボールの手前に出る → 画面上のボールの円で隠すようにした
  - STROKEカップ確認でガイドが画角に入らない → 線の長さは変えず、置く位置を狙い方向へずらした
- 実機確認で「打つ前に全体像が分からない」と指摘され、**T3.6 コースマップを実装済み・実機確認OK**
  - ADDRESSの5つ目の視点ボタン「マップ」からコース全体を真上から見渡す
  - 枠取りは `course.bounds`。16m × 24m は縦画面（390×844）で視距離35.9m・X±8.64m / Z±18.70mに収まる
  - ボール（白）とカップ（赤）は、斜めの矢印とドット文字のラベル（TEE / BALL / CUP）で示す
  - 曲がりの予測線・推奨ルートは描かない。ボールが止まっている間しか見られない
- 現在の試験ホールには以下が入っている
  - 16m × 24m の長方形コース
  - L字ドッグレッグ
  - green / rough / deepRough（セカンドカット）/ water / OB のサーフェス分類
  - 芝は中心線から片側5.6mまでプレイ可能。境界の揺らぎ込みでコース面積のOBは28.9%
  - 摩擦倍率はラフ 3.5倍・セカンドカット 6.0倍（暫定）
  - 池の岸は1mだけラフ
  - 池とOBは高さの濃淡なしの単色。池は見た目だけ0.12m低い
  - 木はコース枠内のOBエリアに立つ
  - 池・OBは1罰打を加えて直前のショット位置へ戻す
  - 打ち切りは無い。ダブルパーに達すると自分から降りられる（ギブアップ）
  - 360°の方向調整
- 実機確認で操作の一貫性とボタン配置を指摘され、**T3.7 を実装済み・実機確認OK**
  - 規則を「進む＝画面タップ / 切り替え・戻る＝ボタン」に統一
  - 読み4視点すべてでタップからSTROKEへ入れる（以前は2視点だけ）
  - マップを視点バーから外し右下の単独ボタンへ。視点ボタンは353px→290pxで折り返さなくなった
  - 開発用の表示切替は `?debug=1` のときだけ表示
  - STROKE中のボタンを下の左右の角へ置いた（右下＝読みに戻る／左下＝狙いを見る）
  - つま先の絵を削除
  - 用語を「視点ボタン＝カメラ位置 / 動作ボタン＝動詞」で統一（方向調整→ボール後方、カップ確認→狙いを見る など）
  - 「狙いを見る」のガイドの手前端を画面下端の外まで伸ばした（奥端は不変）
- 実機確認で「形が幾何学模様すぎる」と指摘され、**T3.8 コース境界の揺らぎと検証器を実装済み・実機確認OK**
  - 芝・ラフ・セカンドカットの幅を位置ごとに増減させ、3本の縁が平行にならないようにした
  - 池の輪郭を真円・真楕円ではなくし、岸の幅も一定でなくした
  - 揺らぎは `CourseDefinition.seed` だけから決まり、`surfaceAt` は座標だけで決まる純関数のまま
  - 調整値は `CONFIG.course.edgeNoise`
  - シードが外形も決めるようにした。`?seed=12345` か `?debug=1` の「シード ⟳」で外形ごと切り替えられる
  - `npm run validate:course` で決定論・ティーからカップまでの芝の連結・面積比を確認できる
    （シード200通りの掃引を含む。全て連結OK、OB面積比は28.5〜34.8%）
  - 面積比は 通常芝21.4% / ラフ21.1% / セカンドカット23.3% / 池5.4% / OB28.9%（揺らぎなしは 21.3 / 18.3 / 23.8 / 5.3 / 31.2）
- **T4 コース生成器と地形の性格を実装済み・実機確認OK**
  - `generateCourse(seed)` がシードひとつから1ホール作る。検証器を通ったものだけを返す
  - 難易度（やさしい/ふつう/むずかしい）× 型（ストレート/ドッグレッグ/S字）を重みで選ぶ
  - 地形の性格を5種類そろえた
    （ランダム / 片流れ / 受けグリーン / ポテトチップ / 2段グリーン）
  - par はルート全長から決まる（境目は変えないまま、T8.1で基準を +1 して 3/4/5 になった）。
    打ち切りはT8.2で廃止した
  - シードを切り替えるとホールごと別のものになる。`?course=prototype` で従来の試験ホール
  - `?debug=1` のとき、状態表示の横に地形の性格を出す
  - `npm run validate:course` に生成200シードの掃引を追加（決定論・連結・面積比とも全てOK）
  - 生成の実測: 平均試行回数2.06回 / OB面積比 18.0〜44.8%（平均33.1%）
  - `terrain` を指定しない従来の地形は、変更前と高さが完全一致（`/green-test/` も不変）
- T4の実機確認で見つかった不具合を修正済み・実機確認OK（PR #35 / #36）
  - シードを切り替えても前のホールのカップが残っていた。`newGreen()` が
    モジュールスコープの `cup` だけ引き直していなかった。マップのピンの位置、
    狙いの初期値、`distanceToCup()` によるカップ後方視点の判定が全て古いカップ基準になっていた
  - ストローク画面に前の一打のパター軌跡が残っていた。`SwipeMeasure.cancel()` が
    サンプルのリングバッファを消していなかったので、サンプルも捨てる `reset()` を足した。
    直前のスイングを見せたい `/swipe-test/` は `cancel()` のまま
  - 「狙いを見る」から手元へ戻る操作だけがボタンだったので、タップに統一した。
    重複する「手元を見る」ボタンは削除
  - 「狙いを見る」のガイドがカメラ移動中に出ていた。着いた姿勢を前提に
    手前端を画面外へ置く作りなので、遷移中は出さず着いたフレームで出すようにした
- **T7 複数ホール進行を実装済み・実機確認OK**
  - `Round`（`src/round.ts`）がシードの配列と何ホール目か、各ホールの打数を持つ。
    合計とパー差もここが出す。コースの生成も表示も知らないので、チャレンジにも使える
  - 固定9ホールのコースデータは `src/course/tour-holes.ts` の**セット名とシードの配列**。
    現在は「風の丘」「曲がりの森」「水鏡の庭」の試遊用3セットを置いている。
    各セットはパー3×2・パー4×5・パー5×2（合計パー36）で、パー4はすべて「むずかしい」
  - カップインかギブアップでホールが終わると `HOLE_OUT` へ。
    RESULT の俯瞰と軌跡を背景に残したままスコアカードを重ね、タップで次のホールへ
  - 9ホール終わると `ROUND_END`。一覧表と合計を出し、「もう一度」ボタンだけで進む
  - プレイ中は `HOLE 3/9 ・ PAR 3 ・ 2打 ・ 合計 +2` の1行を常時出す
  - 今までの「同じホールを打ち直す」挙動は**練習モード**として残した。既定は通常ツアー。
    `?mode=practice` のほか、`?seed=` と `?course=prototype` は自動で練習になる。
    シード⟳ボタンは練習モードだけ
- **T7の実機確認で4件の指摘が出た。4件とも片付いた**
  - ①マップの向きが上下逆 → **T7.2で修正・実機確認OK**
  - ②カップのマーカーが分かりにくい（ピンは旗の絵のほうが分かりやすい） → **T7.2で修正・実機確認OK**
  - ③カメラ遷移が激しい → **実機で気にならなくなったので取り下げ。**
    直前の変更のどれで解消したかは特定していないので、今後また気になったら挙げ直す
  - ④ホーム画面に戻るとラウンドが消える → **T7.1で対応・実機確認OK**
- **T7.1 ラウンド進行の保存と再開を実装済み・実機確認OK**
  - T7の実機確認で「iPhoneでホーム画面に戻るとラウンドが最初からになる」と分かった件の対応。
    バグではなく未実装だった（iOS Safari はバックグラウンドのタブを捨てて復帰時に読み込み直す）
  - **再開は現在のホールの頭から。** 保存するのは何ホール目かとホールアウト済みのスコアだけで、
    進行中のホールの打数・ボール位置は持たない（整合を取る箇所が増えて保存漏れがバグになるため）
  - 保存は `src/round-storage.ts` の `RoundProgressStore` と `Round.snapshot()` /
    `Round.restore()`。ラウンドの中身を知らない作りなので、チャレンジの進行と自己ベストでも使う
  - 保存に形式のバージョンとシード列の識別子を入れた。**固定9ホールのシードを選び直したら
    古い保存は黙って捨てて最初から始まる**（壊れた JSON も同じ）
  - 通常ツアーだけ。練習モード（`?seed=` / `?course=prototype`）は保存も復元もしない
  - 復帰後の最初の ADDRESS に `ホール3から再開します` を出す。最初の操作で通常の案内へ戻る
- **T7.2 マップの向きとカップの旗を修正済み・実機確認OK**
  - マップは**常にカップが画面上・ティーが画面下**。向きはホールごとに tee → cup で決める
    （`courseMapRotated`）。回すのはワールドの ±Z のどちらを画面上にするかだけで、
    枠取りの計算はそのまま。ホールの途中でボールが動いても向きは変わらない
  - 生成コースはルートが z:0 → z:total なので9ホールとも上下逆だった。
    試験ホールだけ意図どおりで、T4で生成コースが既定になったときに気づかれず逆転していた
  - 180°回したホールでは +X が画面の左になるので、マーカーの左右反転の判定も画面基準にした
  - RESULT の真上俯瞰（`yawDeg: 0`）は**触っていない**。結果としてカップが上に来る点は同じで、
    上方向がワールド軸か一打ごとかの違いだけ残る
  - カップのマーカーは**旗の絵**にして `CUP` の文字を落とした。旗竿の根元が指し示す点で、
    旗は右へたなびく四角い旗で固定。旗竿はゲーム内と同じ色（`CONFIG.hole.flagstickColor`）
  - **ボール側の矢印とドット文字は変更なし**（粒を細かくする案は文字が小さくなるので取りやめ）
  - 3画面 × 9ホール＋試験ホールの30通りで、投影して**30通りともカップが画面上**を確認済み。
    旗のはみ出しも30通りとも0px
- **T8 カップまわりの物理を実装済み・実機確認OK**（PR #44）
  - カップの縁で速度ベクトルを鏡面反射させる処理を**削除**した。`lipOutDamping` も消した。
    実際のパットで向こう縁に当たって軌道が反転することはまず起きない
  - **旗竿の当たり判定を入れた**（ピンは挿したままの前提）。跳ね返って見えていたものの正体はこれ。
    XZ平面の円柱として、ボール中心が竿の中心から 33.3mm（竿12mm + ボール21.3mm）で衝突。
    速い球がすり抜けないよう**線分と円の交差**で見る。
    法線成分は反転させて 0.25 倍、接線成分は 0.8 倍。
    衝突後 0.6m/s 未満なら口の中なのでカップイン、そうでなければ**当たった側と同じ側へ**出る
  - カップインの速度上限を**横ずれの関数**にした（口を横切る弦の長さに比例。芯で 1.4m/s、
    口の縁で 0）。固定 1.4m/s の崖をやめたので、なめて入る／なめて外れるが特別扱いなしに出る
  - 竿に触れず口を横切って外れる場合は**軌道を反転させない**。少し減速し、
    わずかにカップ側へ曲がってそのまま先へ流れる
  - 数値は全て `CONFIG.physics`（`cupCaptureFalloff` / `cupGrazeSpeedLoss` / `cupGrazeTurn` /
    `flagstickRestitution` / `flagstickTangentKeep` / `flagstickCaptureSpeed`）。
    **初期値は提案。良し悪しは実機で決める**
  - 「なめて入る」を円を描いて落ちるように見せる**演出は入れていない**。今回は物理だけ。
    実機確認で問題なしだったので、**演出は当面入れない**（また気になったら挙げ直す）
  - 検算の数字は `TASKS.md` の T8。短いパット（残り0.5m）は芯なら 0.705〜2.505m/s で入り、
    変更前（0.700〜1.565m/s）より**広がっている**。縁寄り（横ずれ40mm以上）は狭くなった
- **T8.1 パーの基準を全ホール +1 して 3/4/5 に揃えた・実機確認OK**（PR #47）
  - 「結構難しいゲームなので、パーを1つ上げたい。結果として普通のコースと同じパー数になる」
    という判断による。`parFor()` の返す値だけを +1 し、
    **全長の境目（`parThresholds` = 12 / 22 / 30m）は動かしていない**。
    どのホールがどのPARかという区分は変わっていない
  - 生成ホールの全長は最長28mなので、出るPARは **3 / 4 / 5**。「>30mでPAR6」には届かない
  - 手作りの試験ホール（`prototype-course.ts`）も par 3 → 4 に揃えた
  - `CONFIG.game.round.save.version` を 2 に上げた。古いパーのスコアを持つ保存は捨てられる
- **T8.2 打ち切りを廃止し、ギブアップを置いた・実機確認OK**（PR #48 / #49）
  - **公式のゴルフ規則に打ち切りは無い。**`par + 5` の打ち切りはスコアを丸める機能ではなく
    「終われなくなった人の出口」だったので、まず**詰む状態が本当に無いか**を先に確かめた
  - 調査は `npm run check:stuck`（`scripts/check-stuck.ts`、PR #48）。
    シード1〜200・止まれるマス235,344・271万発で、
    **「どう打っても池かOBにしか行けない位置」はゼロ**。
    カップから1m以内で入れられない位置もゼロ。最弱の一打を1.0m/sに固定した感度確認でもゼロ
  - **最強の一打 4.0m/s は仮定**（実機のスワイプ速度は未実測）。
    ただし実際の上限がこれより高いなら脱出できる打ち方が増えるだけなので、仮定は安全側に外れる
  - その上で `strokeLimit()` と `strokeLimitOverPar` を削除（PR #49）。
    罰打の頭打ちも無くなり、結果テキストから打ち切りの3分岐が消えた
  - 代わりに**ギブアップ**。打数がダブルパー（`giveUpParMultiple: 2`）に達したときだけ出す。
    そこまでは画面に出ないので、普通に遊んでいる人の目に触れない
  - ツアーでは**その時点の実打数のまま**記録する（丸めない＝降りても得しない）。
    スコアカードの `*` は残し、文言だけ「打ち切り」→「ギブアップ」に改めた
  - 練習では「ティーへ戻る（打ち直し）」として振る舞い、スコアは記録しない。
    打ち切りが兼ねていた練習の戻り道を、ここで引き継いでいる
  - 誤爆対策は**0.9秒の押し続け**（`giveUpHoldMs`）。確認カードは採っていない
    （「タップで進む」と「タップで取り消す」が同じ画面に並ぶため）。置き場所は左下
- ゲーム全体の基本方針は確定済み
  - 通常ツアー: 固定9ホール
  - 週替わりチャレンジ: 毎週月曜0時（日本時間）更新、全員共通の自動生成3ホール、挑戦回数無制限
  - 通常ツアー / チャレンジは自己ベストとオンラインランキングあり
  - 練習モードはランキング対象外
  - 広告は3ホール終了時、およびラウンド途中から別ゲームを始める直前
  - 広告・同意の共通方針は hanage-hub の `docs/ADVERTISING_POLICY.md`、Putt固有の表示条件は `docs/spec.md` を正とする
  - 利用者向けプライバシーポリシーは `https://hanage.app/privacy/` に集約し、Putt内の導線は正式公開前に追加する

## 実機確認待ち

**試遊用3コースの確認待ち。** T7・T7.1・T7.2・T8・T8.1・T8.2 までは、いずれも実機確認で問題なしと確認済み。
ギブアップ（T8.2）は、普段は目に触れないこと・0.9秒の押し続けで誤爆しないこと・
練習でティーへ戻る動きが自然なことを実機で確認済み。

- `?tour=breeze`（風の丘）
- `?tour=forest`（曲がりの森）
- `?tour=waterside`（水鏡の庭）

各9ホールを通し、違和感のあるホール番号・テーマ名・並びがあれば差し替える。

操作感・見た目はAIだけで合否を決めず、実機確認結果を優先する。

### T7の実機確認で出た指摘の始末

- ラウンドが最初からになる → **T7.1 で対応済み・実機確認OK**
- マップの向きが上下逆 / カップのマーカー → **T7.2 で対応済み・実機確認OK**
- カメラ遷移が激しい → **実機で気にならなくなったので取り下げ**（原因は特定していない）


固定9ホールは機械指標から3セットを仮選出した段階。面白さは実機確認後に確定する。

## 残っている小さな宿題

- 最終ホールのホールアウトのカードを出している最中に離脱すると、ラウンドは最初からになる。
  再開できるホールがもうないため。ROUND_END の復元は今回の範囲に入れていない
- `docs/spec.md` §4 が「手元を見る」ボタン前提の記述のまま。
  実装は「狙いを見る」からタップで戻る形に変わっているので、次に §4 を触るときに揃える
- 生成器が「ティー側から到達できない芝」を作ることがある（シード66・107・149の3/200）。
  池の岸だけが取り残されてまわりが全部OBになる形。
  いまは隙間が0.75m以上あり、1ステップの移動は最強の一打でも1.67cmなので**転がって行けず無害**。
  ただし保証ではないので、池の置き方や `waterFringe` を触るときは
  「到達できない芝を作らない」を検証器の条件に足すこと（PR #48 の調査で判明）
- **実機のスワイプ速度の上下限が未実測。** 詰みの調査（PR #48）で仮定を置くしかなかった。
  測るページは既にある（`/swipe-test/`）。`speedK` を今後いじるときの土台になる

## 次にやること

**次は試遊用3コース・全27ホールの実機確認。**
各セットは普通のゴルフのハーフに合わせてパー3×2・パー4×5・パー5×2とし、
簡単すぎるパー4を避けるため、パー4はすべて生成器の「むずかしい」から選んだ。
**並びを変えるとそのセットの古い保存（T7.1）は捨てられる**点に注意する。

進め方は2段に分ける。

- **T9a: 完了。候補表を機械的に作った。** シードを掃引して、難易度・型・地形・PAR・距離・遠回り率・
  池の数・OB面積比を一覧にし、PARと地形で偏りなく30本前後へ絞る。
  各行に `?seed=N` の実機リンクを付ける（`?seed=` は練習モードで単独ホールを開く）。
  **面白いかどうかは書かない。**測れた事実だけを書く
- **T9b: 3セットを仮選出して実装。** 「風の丘」「曲がりの森」「水鏡の庭」の3セットを
  `tour-holes.ts` へ入れた。人間が各9ホールを通して遊び、違和感があれば差し替えて確定する

セットのテーマ名は仮。実際の遊び味と合わなければ、ホールと一緒に見直す。

1. ~~コース生成器（T3.8 の検証器・T4 の生成器で完了）~~
2. ~~複数ホール進行（T7で完了）~~
3. ~~ラウンド進行の保存と再開（T7.1で完了）~~
4. ~~マップの向きとカップの旗（T7.2で完了）~~
5. ~~カップまわりの物理（T8で実装。旗竿の当たり判定と反発の減衰、縁の鏡面反射の削除。実機確認OK）~~
6. ~~パーの基準を +1 して 3/4/5 に揃える（T8.1で完了。実機確認OK）~~
7. ~~打ち切りの廃止とギブアップ（T8.2で完了。詰みが無いことを確かめた上で廃止。実機確認OK）~~
8. 固定9ホールの選定（T9a 候補表 → T9b 3セット仮実装。**現在は実機確認待ち**）
9. ~~週替わりチャレンジのseed確定・保存構造・再現~~（コア実装済み。進行保存の接続と画面は後続）
10. 自己ベストの保存とランキング
11. ゲーム内から `https://hanage.app/privacy/` を開ける導線
12. 広告を含むゲーム開始・離脱・完了遷移

詳細な背景・タスク履歴は `TASKS.md` を参照する。
初回公開までの工程・依存関係・完了条件は `RELEASE_PLAN.md` を参照する。

## まだ未実装の主なもの

- 試遊用3コースの最終確定（現在は仮選出した全27ホール）
- 週替わりチャレンジのメニュー・画面遷移・進行保存の接続（週次生成コアは実装済み）
- 自己ベストの保存・オンラインランキング（ラウンド途中の進行の保存はT7.1で実装済み）
- プライバシーポリシーへのゲーム内導線
- 広告を含む完成版のゲーム遷移

## セッション開始時

1. GitHubの最新 `main` とオープン中のPRを確認する
2. この `PROJECT_STATUS.md` を読む
3. `RELEASE_PLAN.md` で残工程と依存関係を確認する
4. 使用するAIの入口（`CLAUDE.md` または `AGENTS.md`）を読む
5. 必要な範囲だけ `TASKS.md` と `docs/spec.md` を読む
6. 最新 `main` からタスク専用ブランチを作る

チャットの過去ログや、pushされていないローカル状態だけを引き継ぎ元にしない。

## セッション終了時

- 「現在地」「実機確認待ち」「次にやること」が変わった場合は、このファイルを更新する
- 残工程・依存関係・初回公開の完了条件が変わった場合は `RELEASE_PLAN.md` を更新する
- ゲーム仕様・UXを変えた場合は `docs/spec.md` を同期する
- タスク構成・履歴・実装順を変えた場合は `TASKS.md` を同期する
- 恒久的なAI向けルールが変わった場合だけ `CLAUDE.md` / `AGENTS.md` を更新する
- ブランチ、PR、確認内容をGitHub上に残し、次のAIが再開できる状態にする
