# 初回公開までの計画 / Release Plan

最終更新: 2026-09-05

このファイルは、アプリを「初回公開できる状態」まで進めるための工程、依存関係、完了条件の正本。
日々の現在地は `PROJECT_STATUS.md`、詳しい実装履歴は `TASKS.md`、未確定の試遊改善は `docs/PLAYTEST_BACKLOG.md`、ゲーム仕様は `docs/spec.md` を参照する。
仕様や履歴をここへ複製せず、各工程の状態と次へ進める条件だけを管理する。

## 状態の表し方

- `未着手`: まだ作業を始めていない
- `進行中`: 作業中、または人の確認待ち
- `ブロック`: 先に決めること・終えることがある
- `完了`: 実装、必要な自動確認、必要な実機確認が終わり `main` に入っている

## AdSense審査へ載せるための中間マイルストーン

**Puttの完全完成をAdSense審査の前提にはしない。**
AdSense申請は `hanage-hub` と Multicolor Sweeper を主力に進め、Puttは「通常ツアーが安定して遊べる掲載作品」として先に審査へ載せられる状態を作る。

この中間マイルストーンでは、以下をPutt側の完了条件とする。

- 通常ツアーをトップから開始し、9ホール終了後まで主要導線が破綻せず遊べる
- プレイ中の確認付きトップ復帰、コース選択、再開、終了後の戻り動線が主要端末で確認できている
- トップから簡単な遊び方を確認できる
- ゲーム内から `https://hanage.app/privacy/` を開ける
- 未接続の週替わりチャレンジは、審査時の公開トップで「準備中」を目立たせず一旦非表示にする
- 通常利用で開発用UIが露出せず、リンク切れ・空ページ・行き止まりがない
- OB境界など、通常プレイに直接支障する分かりにくさが残る場合は最小限の修正を行う
- 公開URLから遊べ、hanage.app から到達できる

**この中間マイルストーンのブロッカーにはしないもの:** 固定ホールの全面再選定、コース生成器v2、専用練習グリーン、パター感度・グラフィック改善、転がりスキップ、週替わりチャレンジの画面接続、ランキング、ゲーム内広告、PWA化。

未確定の改善点は `docs/PLAYTEST_BACKLOG.md` に保持し、審査を待たせない。

## 初回公開の完了条件

以下をすべて満たした時点を、このリポジトリにおける初回公開完了とする。
AdSense審査用の中間マイルストーンより後の、Putt単体としての完成条件である。

- 通常ツアーの固定9ホールが暫定値ではなく、実際に遊んで選んだ構成になっている
- 週替わりチャレンジが毎週月曜0時（日本時間）に全ユーザー共通の3ホールを出し、同じ週なら再現できる
- 通常ツアーと週替わりチャレンジで自己ベストを保存し、オンラインランキングへ登録・表示できる
- 練習モードを含む各モードへ、開発用URLを直接入力しなくても移動できる
- `docs/spec.md` どおりの広告タイミングと、別ゲーム開始・途中離脱・完了の遷移が成立する
- 広告やランキングが一時的に失敗しても、ゲーム本体を遊べなくならない
- 通常利用で開発用UIが露出せず、スマートフォン縦画面の主要導線を実機確認している
- 公開URLから遊べ、ハブサイトから到達できる。広告・ランキングで扱うデータとプライバシー方針の案内先が整合している
- `npm run build` と既存の検証スクリプトが通り、`PROJECT_STATUS.md` と関連仕様が最終状態に同期している

インストール可能なPWA化、見た目の大幅刷新、サウンド追加は、別途仕様へ追加しない限り初回公開の必須条件に含めない。

## 残工程

| 工程 | 状態 | 内容 | 次へ進める条件 | 依存 |
|---|---|---|---|---|
| 審査掲載準備 | 進行中 | 通常ツアーの基本導線は実装・実機確認OK。プライバシー導線、簡単な遊び方、週替わり「準備中」の公開トップ非表示、必要ならOB境界の小改善が残る | 中間マイルストーンの条件を満たし、公開URLとハブ導線を実機で一巡する | 現在の通常ツアー |
| 固定9ホール | 進行中 | T9aの候補30本作成とT9bの試遊用3コース・全27ホール仮選出まで完了。試遊で風の丘H5〜H7の類似、曲がり形状の人工感などを確認 | 初回公開までに再試遊し、最終採用するホール・並び・テーマ名を確定する。AdSense審査開始のブロッカーにはしない | `docs/PLAYTEST_BACKLOG.md` |
| 週替わりチャレンジ | 進行中 | 週キーから共通の3シードを決め、PAR3/4/5を1本ずつ生成するコアは実装済み。進行保存・再開と画面への接続が残る | 同じ週の再現、週変更、再挑戦、復元、画面遷移を確認 | 固定9ホールとのシード非重複は検証で保証 |
| 自己ベスト・ランキング | 未着手 | 通常ツアーと週替わりチャレンジの自己ベスト保存、オンライン登録・表示、失敗時の継続 | スコア識別子、保存形式、登録・取得、異常系を確認 | チャレンジの識別方法。バックエンド方針の決定 |
| モード選択・広告・遷移 | 進行中 | トップメニュー、通常ツアー3コース選択、練習入口、プレイ中の確認付きトップ復帰、ツアー/練習終了後の戻り動線を実装。週替わり接続、別ゲームへの途中切替、広告が残る | 初回公開では仕様の主要遷移を実機で一巡できる | 週替わり接続とランキングの画面・状態が確定 |
| 公開前仕上げ | 未着手 | 文言・画面配置・失敗時表示・デバッグ露出・主要端末の確認 | 公開前チェックをすべて完了 | 上記機能の統合 |
| 公開 | 未着手 | 本番URL、ハブ導線、プライバシー案内、公開後の最低限の動作確認 | 公開URLで主要導線が通る | 公開前仕上げ |

## 現在の優先順

AdSense審査開始を先に進めるため、当面は次の順序を優先する。

1. **Puttの審査掲載準備**
   - `https://hanage.app/privacy/` へのゲーム内導線
   - トップから見られる簡単な遊び方
   - 未接続の週替わりチャレンジを公開トップから一旦隠す
   - 必要ならOB境界の最小限の視認性改善
   - 公開URL・主要端末・ハブ導線の確認
2. **AdSense審査へ進む**
   - 申請全体の正本は `hanage-hub` 側
   - Multicolor Sweeperを主力とし、Puttは通常ツアーが安定して遊べる作品として掲載する
3. **審査を待たせない品質改善**
   - 固定9ホールの差し替え・並べ替え
   - コース生成器v2
   - 練習グリーン専用化
   - パター感度・パターヘッド・転がりスキップ
4. **Putt初回完成へ向けた機能統合**
   - 週替わりチャレンジの画面接続
   - 自己ベスト・ランキング
   - 広告を含む完成版遷移

週替わりチャレンジのコアは固定9ホールと別モジュールで実装済み。
候補シードは現在の固定27ホールを除外し、`src/course/tour-holes.ts` の変更時にも検証で重複を検出する。

トップメニューと通常ツアー／練習の入口、ゲーム中の確認付きトップ復帰、ツアー／練習終了後の戻り動線は実装・実機確認済み。

## 更新ルール

- セッション開始時に `PROJECT_STATUS.md` とこのファイルを確認する
- 工程が始まった、ブロックした、完了したときにこの表の状態を更新する
- 現在作業中の1件と直近の次作業は `PROJECT_STATUS.md` にも反映する
- 未確定の試遊改善は `docs/PLAYTEST_BACKLOG.md`、詳細な実装判断は `TASKS.md`、ゲーム仕様の決定は `docs/spec.md` に記録する
- 1つの検証テーマにつき1ブランチ / 1PRとし、並行ブランチ間で同じファイルを極力触らない
- 操作感・見た目・週の境界など体験を変える判断は、選択肢と推奨案を示して人が決める
