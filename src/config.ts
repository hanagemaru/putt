// チューニング可能な数値は全てここに置く。他のファイルにマジックナンバーを書かない。
// 単位はメートル・秒。角度は内部ではラジアン。

/** 単位の定義であってチューニング値ではない */
export const FEET_TO_METERS = 0.3048;

export const CONFIG = {
  green: {
    /** グリーンの一辺 [m] */
    size: 20,
    /** 表示メッシュの分割数 */
    segments: 128,
    /**
     * 芝の基準色。濃淡は**この色を中心に**上下へ振れる。
     * 暗いところで読めるだけの明暗差を出すには、平均の明るさそのものが要る
     * （実測：芝の平均輝度 45/255 → 105/255）
     */
    color: 0x74cf5c,
    /** 物理・描画の唯一の情報源であるハイトマップの解像度（正方 res×res） */
    heightmapResolution: 256,
    /** 手続き生成のシード。同じ値なら同じ地形になる */
    seed: 20250823,
    /**
     * 全体傾斜 [%]（spec §1 は 1〜3%）。向きだけシードから決める。
     * 20m 一辺に対して一様にかかるので、これが大きいとグリーンの半分がずっと下りになる。
     * 2% だと転がりの 90%点が 4.9m → 5.5m に伸び、頂点カラーの濃淡も傾斜に食われて
     * うねりが読めなくなるので、範囲の下端に置いている
     */
    tiltPercent: 1,
    /** うねりの振幅 [m]。合成したガウシアンをこの振幅（±）へ正規化する */
    undulationAmplitude: 0.15,
    /** 合成するガウシアンの数 */
    gaussianCount: 7,
    /**
     * ガウシアンの広がり [m] の範囲。小さいほど局所的に急な斜面ができる。
     * 振幅は正規化されるので、ここが「どれだけ急な面ができるか」を決める。
     * 勾配が MU*7/(5g)（スティンプ 10ft で約 7.8%）を超える面がないと
     * 「下りで止まらない」が起きないので、下限は狭めに取ってある
     */
    gaussianSigmaMin: 0.7,
    gaussianSigmaMax: 3.5,
    /** ガウシアンの中心を置く範囲。グリーンの一辺に対する割合 */
    gaussianSpread: 0.9,
    /**
     * 頂点カラーの濃淡（§1 のアンジュレーション表現の主軸）。
     * 表すのは「高さ」ではなく「斜面の向きと強さ」。一人称の視野に入るのは半径数メートルで、
     * そこの高低差は数センチしかない（実測：4.5m のライン上で 1.6cm ＝ 明度差 3%）。
     * 高さでは表示できないが、同じライン上の勾配は 0.8%→2.3% と変化している
     */
    shade: {
      /**
       * 勾配の濃淡の振れ幅（±）。0 で単色。
       * 明るさの係数は 1 を中心に ±この値だけ振れる。上りは明るく、下りは暗くなる。
       * 「基準から暗くする」片側だけの式だと、濃くするほど画面全体が沈んで暗所で真っ先に潰れる
       * （実測：平均輝度 45/255、明暗の幅は p5-p95 で 14 段しかなかった）
       */
      gradientStrength: 0.75,
      /**
       * 濃淡がフルレンジで表す勾配（無次元、0.03 で 3%）。**読みの単位を決める値。**
       * シードによらず「明るさがこれだけ変わる ＝ 勾配が何 %」を一定にする。
       * 臨界勾配（スティンプ 10ft で 7.8%）に合わせて 0.08 にしていたが、
       * 実際のグリーンの勾配は中央値 1.7%・p75 で 2.4% しかなく、濃淡のレンジの中央付近しか
       * 使われていなかった（実測：明暗の幅が 9 段）。実際に出る勾配の側へ合わせて 0.03 にする。
       * これでも 6% 付近まで緩やかに効き続ける（softRamp の裾）
       */
      gradientRange: 0.03,
      /** 光の方位 [度]。太陽に相当する固定方向。プレイヤーが回っても地形の見え方が変わらないように固定 */
      lightAzimuthDeg: 35,
      /** 高さの濃淡の振れ幅（±）。地形のマクロな高低が消えないよう弱く重ねる */
      heightStrength: 0.12,
      /** 高さの濃淡がフルレンジで表す高低差 [m] */
      heightRange: 0.5,
      /**
       * 明るさの係数を丸める段数（レトロ表現の試作）。0 で丸めない。
       * 段にすると濃淡がグラデーションではなく帯になる。1段ぶんの明暗差は一定の勾配差なので、
       * 絶対スケールの約束は保たれる（段の数がそのまま勾配の目盛りになる）
       */
      levels: 0,
    },
  },

  hole: {
    /** カップの直径 [m]（規定 108mm） */
    diameter: 0.108,
    /** カップの深さ [m] */
    depth: 0.1,
    /** カップの位置 [m]。XZ 平面 */
    position: { x: 0.6, z: -5.5 },
    /** 旗竿の高さ [m]。必ず鉛直に立てる（傾き表現の基準） */
    flagstickHeight: 1.5,
    flagstickRadius: 0.008,
    flagWidth: 0.32,
    flagHeight: 0.22,
    flagColor: 0xd94f3d,
    cupColor: 0x121a12,
    /** カップの縁の色と幅 [m]。芝を切った断面。遠くからカップを見つける手がかりになる */
    rimColor: 0xdfe7d8,
    rimWidth: 0.012,
    /** 穴に見せる円を芝から浮かせる量 [m]。Z ファイティング防止 */
    mouthLift: 0.004,
  },

  ball: {
    /** ゴルフボールの半径 [m]（直径 42.7mm） */
    radius: 0.0213,
    color: 0xf6f8f4,
    /**
     * 真下に敷く影（§3 FOLLOW の見やすさ）。接地点が分かると距離と曲がりが読める。
     * 影の計算はしない。半透明の円を1枚置くだけ
     */
    shadowScale: 1.6,
    shadowOpacity: 0.28,
    /** 芝からの浮かせ量 [m]。Z ファイティング防止 */
    shadowLift: 0.002,
  },

  /** グリーンの外に敷く地面。グリーンが宙に浮いて見えないようにするだけ */
  surround: {
    size: 60,
    color: 0x2f5d2a,
    /** グリーンの最低点からどれだけ下げるか [m] */
    drop: 0.35,
  },

  /** 背景の木。傾きの基準になるので必ず鉛直に立てる */
  trees: {
    count: 14,
    /** グリーン中心からの距離 [m] の範囲 */
    radiusMin: 13,
    radiusMax: 18,
    heightMin: 3.5,
    heightMax: 6.5,
    trunkColor: 0x5b4632,
    leafColor: 0x2f6b32,
  },

  physics: {
    /** 固定タイムステップ [s]。決定論的に保つ（後でリプレイに使う） */
    timeStep: 1 / 240,
    /** 重力加速度 [m/s^2] */
    gravity: 9.81,
    /** 勾配による加速度の係数。転がる球なので 5/7 */
    slopeFactor: 5 / 7,
    /** スティンプ値 [ft]。摩擦 MU はここから逆算する */
    stimpFeet: 10,
    /**
     * スティンプメーターの解放速度 [m/s]。
     * 「この初速で stimpFeet だけ転がる」から MU = v^2 / (2 * 距離) を出す
     */
    stimpReleaseSpeed: 1.83,
    /** これを下回ったら停止判定に入る [m/s] */
    stopSpeed: 0.02,
    /** カップ中心からこの距離に入ったら判定 [m]（54mm） */
    cupCaptureRadius: 0.054,
    /** この速度未満ならカップイン。以上ならリップアウト [m/s] */
    cupCaptureSpeed: 1.4,
    /** リップアウト時に残る速度の割合 */
    lipOutDamping: 0.7,
    /** 1フレームで進めるシミュレーション時間の上限 [s]。タブ復帰時の暴走を防ぐ */
    maxStepPerFrame: 0.1,
    /** 軌跡を記録する間隔（物理ステップ数）。8 なら 30Hz */
    pathSampleSteps: 8,
  },

  camera: {
    fov: 70,
    near: 0.05,
    far: 200,
    /** 視点の高さ [m] */
    eyeHeight: 1.6,
    /** 原点からの距離 [m] */
    distance: 6,
  },

  light: {
    /**
     * 全体を一様に明るくするだけなので、明暗の比（＝読みの単位）は変わらない。
     * sRGB は明るい側ほど段差が細かいので、平均が明るいほど同じ比でも見える差が大きくなる
     */
    directionalIntensity: 1.75,
    ambientIntensity: 0.55,
    /** 光源の方向（正規化前）。低い角度から当てて陰影で形を出す */
    directionalDirection: { x: 1, y: 0.6, z: 0.5 },
  },

  /**
   * レトロなドット感（試作）。低い解像度に描いてから NearestFilter で引き伸ばす。
   * グリーンのグラデーションだけでなく、ボール・旗・木もまとめて粒が揃う。
   * HUD とストロークのオーバーレイは DOM / 2D キャンバスなので、これまで通りの解像度のまま
   */
  pixel: {
    /** 何分の1の解像度で描くか。1 で等倍（＝ドット感なし） */
    scale: 4,
    /** ドット感を出すときに使う色の段数（shade.levels に入れる値） */
    levels: 8,
  },

  renderer: {
    background: 0x87b7e0,
    /** 端末の devicePixelRatio の上限。上げすぎると重い */
    maxPixelRatio: 2,
  },

  /** ゲーム本体（状態機械とカメラ、spec §3）。検証ページとは共有しない */
  game: {
    /** ボールの開始位置 [m]。カップ (0.6, -5.5) まで約 4.5m */
    ballStart: { x: 0, z: -1 },

    aim: {
      /**
       * 狙いを左右スワイプで変える感度 [rad/px]。
       * 画面幅いっぱい（約 390px）を振って ±22 度くらいになる大きさ
       */
      sensitivity: 0.002,
      /** ボール→カップ方向からの狙いの振れ幅の上限 [度] */
      maxOffsetDeg: 45,
    },

    /** タップとスワイプの区別。READ / ADDRESS / RESULT で使う */
    tap: {
      /** これ以下の移動ならタップ [px] */
      moveMaxPx: 12,
      /** これ以下の時間ならタップ [ms] */
      holdMaxMs: 400,
      /** 視点切り替えとみなす横スワイプの最小移動量 [px] */
      swipeMinPx: 40,
    },

    /** READ の4定点カメラ（§3）。高さはグリーン面からの視点高さ [m] */
    read: {
      /** ボール後方。カップ方向を向く（デフォルト） */
      behindBallDistance: 1.5,
      behindBallHeight: 1.6,
      /** カップ後方。ボール方向を向く */
      behindHoleDistance: 1.5,
      behindHoleHeight: 1.6,
      /**
       * カップ後方だけラインから横へずらす [m]。
       * 真後ろに立つと旗竿とカップがちょうどボールに重なって、ボールが見えない
       */
      behindHoleSideOffset: 0.5,
      /** しゃがんでラインを低く見る */
      lowLineDistance: 1,
      lowLineHeight: 0.4,
      /** ボールとカップの中間点の横。ラインを横から見る。実際にはここか、2点が収まる距離の遠いほう */
      sideMidOffset: 2,
      /**
       * 横から見るときの余裕。ボール→カップの長さに掛けて、画面に収まる距離を出す。
       * 縦画面は横の画角が狭い（fov 70度・アスペクト 0.46 で片側 18度）ので、
       * 固定の距離だとラインが画面からはみ出して、ボールもカップも見えない絵になる
       */
      sideMidFitMargin: 1.25,
      sideMidHeight: 1.6,
      /** 視点を切り替えるときの補間時間 [s] */
      transition: 0.35,
    },

    /**
     * ADDRESS（§3）。狙いを合わせる。
     * 傾けた視野で狙いを合わせても遊びとして意味がなかったので、ROLL は持たない。
     * ボールの後方に立って、狙った方向をまっすぐ見る。
     */
    address: {
      /** 視点高さ [m] */
      eyeHeight: 1.5,
      /**
       * ボールより後方へ引く量 [m]。立ち位置。
       * ボールが画面下端の内側に入る距離。近すぎると足元しか映らず、ボールが画面から外れる
       */
      back: 1.5,
      /**
       * 注視点をボールから狙い方向へ進める距離 [m] の下限。
       * ふだんはカップまでの距離を使う（＝狙った先にピンが見える）。
       * カップが近いときにこれ以上は下げないための下限
       */
      lookDistanceMin: 2,
      /** READ からの遷移時間 [s] */
      transition: 0.4,
    },

    /** STROKE（§3 / §4）。真下を見下ろす */
    stroke: {
      /** ADDRESS ⇄ STROKE の遷移時間 [s]。0.25 秒以内。ロール回転を長く見せない */
      transition: 0.22,
      /** ボール真上の視点高さ [m] */
      eyeHeight: 1.5,
      /**
       * フェース角を目標角へ寄せる時定数 [s]（§4.4）。
       * 最小二乗フィットの結果をそのまま代入するとかくかく動くので、
       * angle += (target - angle) * (1 - exp(-dt / tau)) で滑らかにする。
       * 検証ページ（/swipe-test/）は計測のばらつきが見える利点があるのでそのまま
       */
      faceSmoothingTau: 0.06,
      /** つま先の描画 [px]。画面下端からの位置と大きさ */
      toeWidthPx: 74,
      toeHeightPx: 30,
      toeGapPx: 26,
      toeBottomPx: 18,
    },

    /** FOLLOW（§3）。顔を上げて追う。カメラは平行移動しない */
    follow: {
      /**
       * 打った直後、ボールが STROKE の視界から出るまでは視点を動かさない。
       * すぐに顔や目線を動かさないのが安定したストロークの所作なので、そこに合わせる
       */
      holdUntilOffscreen: true,
      /** 画面外と判定するまでの余裕（NDC）。0.05 なら画面の 5% ぶん外へ出てから */
      offscreenMargin: 0.05,
      /** 保険。ボールが画面内で止まってしまった場合でもこの秒数で顔を上げる [s] */
      holdMax: 2,
      /** ボールが視界から出てから、視点が上がりきるまでの時間 [s] */
      riseTime: 0.15,
      /**
       * 顔が上がったあとの追従の時定数 [s]。0 だとボールが常に画面中心に貼りつき、
       * 周りの絵も動かないので実質静止画に見える。人間の目もボールを完全に中心には保てない
       */
      trackingTau: 0.25,
      /** ボールを画面中央より下に置く角度 [度]。進む先が見えるようにする */
      leadDeg: 8,
      /** ボールが近いときの FOV [度] */
      fovNear: 70,
      /**
       * ボールが遠いときの FOV [度]。目を凝らす表現。
       * 絞りすぎると見かけの大きさまで変わらなくなって静止感が出るので、緩めにしてある
       */
      fovFar: 50,
      /** FOV を絞りはじめる距離 [m] */
      fovNearDistance: 1.5,
      /** FOV を絞りきる距離 [m] */
      fovFarDistance: 9,
    },

    /** CUP（§3）。決着。カット（補間しない） */
    cup: {
      /** ボールがカップまでこの距離を切ったら切り替える [m] */
      triggerDistance: 1.5,
      /** カップ後方へ引く距離 [m] */
      back: 1.3,
      /** ラインから横へずらす距離 [m]。旗竿が画面中央でボールを隠すのを避ける */
      sideOffset: 0.4,
      /** 芝の高さの視点 [m] */
      height: 0.05,
      /** 注視点をカップより少し上に置く [m]。真横だと画面の下半分が芝で埋まる */
      lookAtHeight: 0.03,
    },

    /** RESULT（§3）。ボールが完全に停止してから俯瞰へ。走行中は絶対に切り替えない */
    result: {
      /** 停止を確認してから遷移を始めるまでの待ち [s] */
      settleDelay: 0.35,
      /** 俯瞰への遷移時間 [s] */
      transition: 0.9,
      /** 見下ろし角 [度]。45 度の斜め俯瞰 */
      pitchDeg: 45,
      /** 俯瞰の FOV [度] */
      fov: 55,
      /**
       * ラインの真後ろからの振り角 [度]。真後ろから見るとラインが線に潰れて曲がりが読めないので、
       * 少し回り込んで見る
       */
      yawDeg: 30,
      /** 打ち出し位置・停止位置・カップの広がりに対する視距離の倍率。3点が画面に収まるように取る */
      distanceScale: 2,
      /** 視距離の下限 [m]。短いパットで寄りすぎないように */
      distanceMin: 4,
      /** 軌跡の色 */
      trailColor: 0xffe66d,
      /** 軌跡をグリーン面から浮かせる高さ [m]。Z ファイティング防止 */
      trailLift: 0.02,
      /** 軌跡の頂点バッファ長 */
      trailMaxPoints: 4096,
    },
  },

  /** /green-test/ のグリーン・物理の確認ページ専用。ゲーム本体では使わない */
  greenTest: {
    /**
     * 俯瞰カメラ。20m 四方のグリーンは縦画面の横幅に透視投影では収まらないので、
     * グリーンにフィットさせた正射影で見る。確認用の固定カメラで、ゲーム本体には持ち込まない
     */
    /** 見下ろし角 [度]。90 で真上 */
    cameraPitchDeg: 62,
    /** カメラを置く距離 [m]。正射影なので見た目の大きさには効かない。near/far に収まればよい */
    cameraDistance: 40,
    /** 画面の横幅に収める範囲 [m]。グリーンの一辺より少し広く取って外周も見せる */
    viewWidth: 23,
    /** ボールの初期位置 [m] */
    ballStart: { x: -1.5, z: 5.5 },
    /**
     * 初速の初期値 [m/s]。2.0 で平坦なら 3.6m 転がる。
     * 3.2 だと平坦でも 9.3m 転がり、20m のグリーンでは半分近くが場外に出る
     */
    initialSpeed: 2.0,
    /** 方向の初期値 [度]。0 が -Z（画面奥）、+ が右回り */
    initialDirectionDeg: 0,
    /** 見やすさのためにボールを実寸より大きく描く倍率。20m を画面幅に収めるとかなり小さい */
    ballScale: 15,
    /** 軌跡の色 */
    trailColor: 0xffe66d,
    /** 軌跡をグリーン面から浮かせる高さ [m]。Z ファイティング防止 */
    trailLift: 0.02,
    /** 軌跡の頂点バッファ長。これを超えたぶんは描かない */
    trailMaxPoints: 4096,
    /** 「止まれない面積」を測るときのグリッド分割数。表示用の目安なので粗くてよい */
    severitySamples: 96,
    /** lil-gui のスライダーの範囲 */
    gui: {
      stimpMin: 6,
      stimpMax: 14,
      stimpStep: 0.5,
      seedMin: 0,
      seedMax: 99999,
      seedStep: 1,
      tiltMin: 0,
      tiltMax: 4,
      tiltStep: 0.1,
      amplitudeMin: 0,
      amplitudeMax: 0.5,
      amplitudeStep: 0.01,
      shadeMin: 0,
      shadeMax: 0.8,
      shadeStep: 0.01,
      /** 濃淡のレンジは勾配（無次元）。0.02 で 2%、0.3 で 30% */
      shadeRangeMin: 0.02,
      shadeRangeMax: 0.3,
      shadeRangeStep: 0.01,
      lightMin: 0,
      lightMax: 2,
      lightStep: 0.05,
    },
  },

  /** /swipe-test/ のスワイプ速度計測ページ専用。ゲーム本体では使わない */
  swipeTest: {
    /** ボールを模した円の半径 [px] */
    ballRadius: 28,
    /** インパクト判定に必要な最小バックスイング幅 [px]。指を置いた点から右へこれだけ引くまで打てない */
    minBackswingPx: 40,
    /** パターヘッドのフェース長 [px]（§4.4）。この半分を超えて外すと空振り */
    putterLength: 64,
    /** パターヘッドの厚み [px]。描画のみで判定には使わない */
    putterWidth: 10,
    /** 指を置く前にパターを置いておく位置。ボールの右への距離 [px]（§4.4） */
    putterRestOffsetPx: 36,
    /** 描画するフェース角の上限 [度]。狙い方向（真左）からこれを超えて傾けない。反転もさせない */
    faceMaxAngleDeg: 35,
    /** これ未満のスイング速度では向きを更新しない [px/s]。静止時のジッタ防止 */
    faceMinSpeedPx: 60,
    /** 芯とみなす範囲 [px]。ここまでは減衰なし（§4.5） */
    sweetSpotPx: 12,
    /** フェース端での減衰係数。芯からフェース端までこの値へ線形に落ちる */
    mishitMinGain: 0.55,
    /**
     * スワイプ速度の X 成分 [px/s] → ボール初速 [m/s] の換算係数（§4.6）。
     * 実機で「倍くらい転がってほしい」との判断で 2回、それぞれ √2 倍にした（0.00133 → 0.00188 → 0.00266）。
     * 転がる距離は初速の2乗に比例するので、初速 1.41 倍で距離が 2 倍になる。
     * スティンプを上げても距離は伸びるが、臨界勾配が下がって「下りで止まらない」面が増え、
     * グリーンの性質そのものが変わるので、入力の換算係数の側で合わせている
     */
    speedK: 0.00266,
    /** 打ち出し方向の鈍らせ係数。0 なら常に真左（§4.6） */
    directionSensitivity: 0.4,
    // 以下は検証ページのボール演出（§4.7）。見た目だけの演出で、ゲーム本体の物理とは別物
    /**
     * 演出の画面スケール [px/m]。ボールに寄った近接視点なので、実際のパット程度の初速でも
     * ボールはすぐ画面外へ抜ける。0.3 m/s（ごく弱いタッチ）で画面外へ出る値に合わせてある。
     * 減速と一緒に動かすこと。両方を半分にすると転がる距離は変わらず、見え方だけ緩やかになる
     */
    ballPxPerMeter: 1400,
    /**
     * 転がりの減速 [m/s^2]。演出用に実際のグリーンより抵抗を小さくしている
     * （スティンプ換算で約 20ft 相当。spec §2 の MU＝10ft 相当 0.55 の半分）
     */
    ballDecelMs2: 0.275,
    /** 停止・画面外に出てから中央へ戻すまで [ms] */
    ballResetMs: 900,
    /** 最小二乗フィットに使う、インパクト直前の時間窓 [ms] */
    fitWindowMs: 40,
    /** フィットに必要な最小サンプル数。窓内がこれ未満なら窓を広げる */
    fitMinSamples: 3,
    /** サンプルのリングバッファ長 */
    bufferSize: 512,
    /** 履歴として保持・集計する計測回数 */
    historySize: 10,
    /** 軌跡として描画する直近の時間 [ms] */
    trailMs: 600,
  },
} as const;
