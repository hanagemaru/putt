# タスク分割

**1タスク = 1セッション。** 終わったらセッションを閉じて、次は新しいセッションで始める。
各タスクの「投げる文」をそのままコピーして使う。

---

## T0. 土台と自動デプロイ

これを最初にやる。**実機で触れない状態で先に進まないこと。**

> Vite + TypeScript + three.js のプロジェクトを初期化してください。three.js で緑の平面を1枚表示するだけの最小構成で構いません。
> あわせて GitHub Actions で main への push 時に自動デプロイされる設定を入れてください。デプロイ先は GitHub Pages。
> 縦画面のときは「横にしてください」と表示するオーバーレイも入れてください。
> 完了したら、スマホで開けるURLを教えてください。

完了条件: **スマホのブラウザで緑の面が見える。**

**完了済み。** ただし方針変更があった。当時は横画面前提だったが、現在は**縦画面専用**（`docs/spec.md` §0）。
オーバーレイの反転（横のときに「縦にしてください」を出す）は T1.5 で行う。

---

## T1. スワイプ計測の単体ページ

一番実機での試行回数が要る部分なので、ゲーム本体と切り離して先に作る。

> `/swipe-test` に、スワイプの速度計測だけを検証する単独ページを作ってください。`docs/spec.md` の §4 を読んでください。
>
> - 画面中央にボールを模した円を描く
> - 指を置いて右へドラッグ（バックスイング）、反転して左へ動き、円のX座標を横切った瞬間をインパクトとする
> - `pointermove` を `getCoalescedEvents()` 込みで記録し、インパクト直前40msに最小二乗フィットをかけて速度を出す
> - 計測結果（px/s、スワイプ角度、検出したバックスイング幅）を画面に大きく表示
> - 直近10回の履歴とばらつき（標準偏差）も表示
>
> ゲームのロジックは一切書かないでください。計測だけです。

完了条件: **同じ感覚で10回振って、ばらつきが十分小さい。** ここが安定しないと先に進む意味がないので、必要なら平滑化を調整して繰り返す。

**完了済み。** 実機で確認し、ばらつきは許容範囲。バックスイングのゲート（`minBackswingPx` = 40px）も入れて、
右に引かずに左へ払うだけでは打てないようにした。40px は縦画面での所作として妥当と判断した。

---

## T1.5. パターの所作

ストロークが「振った」だけでなく「当てた」感覚になるかを見る。ここも実機での試行回数が要るので、
ゲーム本体に入る前に `/swipe-test/` で詰める。`docs/spec.md` の §4.4〜§4.7 が該当。

> `/swipe-test/` を拡張してください。`docs/spec.md` の §4.4〜§4.7 を読んでください。
>
> - パターヘッドを、指のY変位に応じて上下に動かす（基準は指を置いた位置）
> - フェースをスイング軌跡と直角に傾ける
> - インパクト時の芯からのズレで初速を減衰させる。フェース端を超えたら空振り
> - インパクトしたらボールが計測速度に応じて左へ転がり、減速して止まる
> - あわせて、横画面のときに「縦にしてください」を出すようにオーバーレイを反転してください
>
> 追加する数値は全て `src/config.ts` に置いてください。ゲーム本体（main.ts）には手を入れないでください。

完了条件: **芯を外した打球が「自分のミス」として納得できる。** 理不尽に感じるなら `sweetSpotPx` と
`mishitMinGain` を調整して繰り返す。あわせて、縦画面の横幅で強い球が打てるかも見る（spec §8 の 2 と 3）。

---

## T2. グリーンと物理

> `docs/spec.md` の §1 と §2 を読んで、グリーンの生成・描画とボールの転がり計算を実装してください。
>
> 確認用に、俯瞰固定カメラでボールを置いて初速と方向を数値入力すると転がる、という開発用ページを作ってください。カメラワークもストロークも今は不要です。
> lil-gui で スティンプ値、ハイトマップのシード、うねりの振幅、濃淡の強さ、ライト強度を調整できるようにしてください。

完了条件: 傾斜で曲がる。スティンプを変えると転がる距離が変わる。上りで止まり、下りで止まらない。

---

## T3. カメラと状態機械

> `docs/spec.md` の §3 を読んで、READ → ADDRESS → STROKE → FOLLOW → CUP → RESULT の状態機械とカメラを実装してください。
> T2 の物理と T1 のスワイプ計測を接続して、1ホールが通しで遊べる状態にしてください。
> 遷移は仕様通りに。特に、走行中に俯瞰へ切り替えないこと。

完了条件: **スマホで一通り打ってカップインできる。**

---

## T4. 調整とデバッグ表示

> `docs/spec.md` の §5 を読んで、デバッグオーバーレイと lil-gui の項目を揃えてください。
> あわせて、指がボールを隠す問題の対策 (a)(b)(c) を切り替えられるようにしてください。

ここから先は実機で触りながら数値を詰める段階。**config.ts の値を変えるだけの調整は、Claude Code に頼まず自分で書き換えたほうが早い。**

---

## T5以降（T4が終わってから決める）

- 軌跡を芝に残す
- モード（ストローク／タイムアタック）
- 読みフェーズの持ち時間
- 見た目の調整

---

# 進め方のルール

## セッションを分ける

タスクをまたぐときは必ず新しいセッションを開く。前のタスクの試行錯誤を引きずらない。
文脈は CLAUDE.md と `docs/spec.md` が持っているので、毎回説明し直す必要はない。

## 仕様は口で伝えずファイルを参照させる

「§4 を読んでください」と書けば済む。仕様そのものをチャットに貼らない。
仕様を変えたいときは、まず `docs/spec.md` を直させてから実装させる。口頭の変更は次のセッションに残らない。

## 大きめのタスクは plan mode で

実装前に計画を出させて、スマホで読んで承認する。方向がずれたまま大量に書かれるのを防げる。

## 実機確認は自分でやる

Claude Code に「動作確認しました」と言わせない。CLAUDE.md にその指示を入れてある。
デプロイURLを開いて自分の指で触る。これが唯一の判定。

## 詰まったら丸投げしない

「なんかスワイプの感じが悪い」ではなく「10回振って標準偏差が18%ある。40msの窓が短すぎる可能性を調べてほしい」と書く。
症状を数値で渡せるように、デバッグ表示を先に作ってある。
