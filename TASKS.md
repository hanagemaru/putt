# タスク分割 / 現在状況

**1ブランチ / 1PR = 1つの検証テーマ。** チャットを継続しても実装単位は混ぜない。
仕様の正本は `docs/spec.md`。恒久的な前提は `CLAUDE.md`。

このファイルは、実装済みの履歴と「次に何をするか」を残す。
古い試行錯誤より**現在の実装を優先**する。

---

## T0. 土台と自動デプロイ

**完了済み。**

- Vite + TypeScript + three.js
- GitHub Pagesへ `main` push時に自動デプロイ
- 現在は**スマホ縦画面専用**
- 横画面では「縦にしてください」を表示

ゲーム本体:
`https://hanagemaru.github.io/putt/`

---

## T1. スワイプ計測の単体ページ

**完了済み。** `/swipe-test/` で実機確認済み。

- `getCoalescedEvents()` を含むポインタサンプルを記録
- インパクト直前40msを最小二乗フィットして速度算出
- 判定ラインとの交差時刻を線形補間
- 直近10回の履歴・ばらつきを表示
- バックスイングなしでは打てないゲートを実装
- 現在の `minBackswingPx = 20`

旧記録の40pxは現在値ではない。

スワイプ計測:
`https://hanagemaru.github.io/putt/swipe-test/`

---

## T1.5. パターの所作

**完了済み。** `/swipe-test/` の基本計測は実機確認済み。ゲーム本体では後から入力原点を改善している。

- フェースがボール右端に触れた位置をインパクトとする
- 指のY変位で芯からのズレを表現
- 芯を外すと初速を減衰、フェース端を超えると空振り
- インパクト後もパターは指の移動量に追従
- ボールを追い越さないよう位置を頭打ち
- `/swipe-test/` の待機位置は `putterRestOffsetPx = 36`
  - ボール半径28px + パター半幅5px + 隙間3px
- ゲーム本体では3Dボールの見かけ半径に合わせ、**ボール表面から約3px後ろ**へ待機させる
- ゲーム本体では pointerdown の絶対位置へパターを移動しない
  - pointerdown時の待機パター位置を原点にする
  - 以後は指の **ΔX / ΔY** だけパターへ反映
  - 速度・角度・バックスイング幅は平行移動で変わらないため、既存 `SwipeMeasure` の計測方法は維持

現在の主要値:

- `putterLength = 64`
- `putterWidth = 10`
- `sweetSpotPx = 12`
- `mishitMinGain = 0.55`
- `faceMaxAngleDeg = 35`
- `speedK = 0.00266`
- `directionSensitivity = 0.4`

`/swipe-test/` のボール転がりは入力確認用の演出で、ゲーム本体の物理とは別物。

---

## T2. グリーンと物理

**完了済み。**

完了条件:

- 傾斜で曲がる
- スティンプを変えると転がる距離が変わる
- 上りで止まり、勾配が摩擦を上回る下りでは止まらない

### 現在の濃淡方針

**高さベースが正式採用。**

- 明るい = 高い、暗い = 低い
- グリーンごとの最小最大で正規化しない絶対スケール
- `gradientStrength = 0`
- `heightStrength = 0.75`
- `heightRange = 0.25`
- 勾配ベースの表示は比較用コードとして残すがゲーム本体では使わない

過去に勾配ベースも試したが、明るさが高さを意味しなくなり、実機で曲がる方向の読み違いが起きたため現在は採用していない。

### 現在のドット表現

- `pixel.scale = 3`
- `pixel.levels = 24`
- 1段 ≒ 0.7cm
- `pixel.colorLevels = 48`

16/32では同色に見える範囲にも実際の高低差が残ることがあったため、24/48へ細分化した。

---

## T3. カメラと状態機械

**完了済み。** スマホで1ホールを通して遊べる状態。

現在の状態機械:

```text
ADDRESS（読み＋方向調整） → STROKE → FOLLOW → CUP → RESULT → ADDRESS
```

旧 `READ → ADDRESS` の分離フローは廃止済み。

### ADDRESS

- 初期画面は方向調整
- 読みと方向調整を同一フェーズに統合
- **方向調整 / 低い視点 / カップ後方 / 横から**、の順でボタン表示
- カメラ切替をスワイプでは行わない
- 方向調整と低い視点では約0.5mの短い直線ガイドを表示
- ガイドは打ち出し方向だけを示し、傾斜・曲がりを予測しない
- 低い視点でも左右スワイプでaim変更可能
  - 通常の方向調整と同じく、**ボールを中心にカメラ位置もaim方向へ旋回**
  - 低い高さ・近い距離だけLOW_LINE専用値を使う
  - タップでそのままSTROKEへ進める
- カップ後方は低い位置・ボールとカップを結ぶ線上から見る。旗竿/旗を薄くしてボールを透かす

### STROKE

- 真下視点
- カップ・旗竿は強制的に隠さない。近距離で自然に視野へ入れば見える
- 「方向調整に戻る」ボタンあり
- パターの表示角だけ感度0.6、時定数0.10秒で平滑化
- 実際の打ち出し方向には表示用平滑化を掛けない
- 待機中のパターはボール表面から約3px後ろ
- pointerdownだけではパターを動かさず、その後の指のΔX/ΔYに同期して動かす
- 「カップ確認」でボール真上の位置を保ったまま現在のaim方向を見る
- カップ確認中も方向調整と短いガイドを使える

### FOLLOW

- カメラ位置は固定し、ヨー・ピッチで追う
- STROKE画面上でボール中心が画面端の約82%へ来た時点でFOLLOWへ切替
- ボールが画面外へ消える前に現在位置へ即座に向ける
- 以後は時定数0.25秒で少し遅れて追従
- FOVは距離に応じて70° → 50°

### RESULT

- ボールが完全停止してから表示
- 軌跡を黄色線で表示
- カメラは**ほぼ真上（pitch 89.9° / yaw 0°）**
- 斜め45°俯瞰は旧仕様

---

## T3後の実機調整

### 確定済み

- ドット濃淡を `levels 24 / colorLevels 48` へ変更
- RESULTを真上俯瞰へ変更
- 旗竿を距離/FOVに応じて表示上だけ太さ補正
  - 基本半径12mm
  - 最大16mm
  - 低解像度画面で最低1.3pxを目安
- STROKEのパター待機位置をボール直後へ修正
- 補間時間のあるカメラ遷移を初期値のおおむね2倍へ変更
- CUP自動切替をカップまで約0.5mへ変更
- CUP固定視点でボールがフレームアウトする前にFOLLOWへ復帰
- STROKE中に「カップ確認」を追加
  - ボール真上の位置を保ったまま現在の狙い方向を見る
  - 左右スワイプで方向調整可能
  - 約0.5m補助線を表示
  - 30°傾き視点は実機比較後に**不採用**
- ADDRESSのカップ後方視点は FOV 50°を採用
- 補助線・RESULT軌跡は**高解像度・半透明の滑らかな表示を採用**
- パター反発倍率を実装
  - 基準 `speedK = 0.00266`
  - 0.50×〜1.50×をスライダーで変更
  - ブラウザへ保存
  - 将来は練習モードでユーザーが合わせる想定
- LOW_LINEを方向決定にも使えるよう変更
  - ボール中心でカメラ位置を旋回
  - 左右スワイプでaim変更
  - タップでSTROKEへ直行
- ゲーム本体のパター操作を相対入力へ変更
  - pointerdownではパター位置を変えない
  - X/Yとも指の移動量だけに同期

### 現在の暫定設定 / 実機確認待ち

- アンジュレーションは当面 **強調 ±0.20m + 3D形状だけ1.5倍**に固定
  - 物理と高さ色は ±0.20m
  - 見た目のY形状だけ1.5倍
  - 後で再調整する可能性あり
- 打ち出し方向ガイドは、手前を太く・濃く、遠端を細く・透明にした遠近表現へ変更
- ADDRESSのカップ後方読み視点
  - 高さ0.45m
  - 横ずらし0m（ボールとカップを結ぶ線上）
  - FOV 50°
  - その視点の間だけ旗竿・旗を opacity 0.18へ薄くする
- パター反発の「調整」ボタンはSTROKE中だけ表示

---

## T3.5. 長方形コース基盤と試験ホール

**実装済み・実機確認待ち。**

- 幅と長さを別々に持つ長方形ハイトマップ
- 目標セル間隔から各軸の物理・表示解像度を算出
- ゲーム本体・将来の生成器・検証器で共有する `CourseDefinition`
- 折れ線ルートから通常芝・ラフ・OBを分類し、楕円の池を配置
- 16m × 24mの手作りパー3 L字ドッグレッグ
- 池・OBはショット＋1罰打、直前位置へ戻す
- 1ホール `par + 5`で打ち切り
- 狙い角の±45°制限を外し、360°方向調整に対応

この段階ではコース生成器・検証器・ランキング・広告・複数ホール進行は未実装。

### T3.5の実機確認1回目を受けた調整

**実装済み・再確認待ち。** 実機で「OBになりやすすぎる」と判明したため、芝の階層と表示を見直した。

- セカンドカットを追加。芝は**通常芝 → ラフ → セカンドカット**の3段になり、その外だけがOB
  - `greenWidth 4m` / `roughFringe 0.8m → 1.2m` / `deepRoughFringe 2.4m`（新規）
  - 中心線から片側5.6mまでプレイ可能。コース面積のOBは**62% → 31%**
- 芝の抵抗を上げた
  - ラフ `2.5倍 → 3.5倍`
  - セカンドカット `6.0倍`（新規）
- 池の岸を `waterFringe 1m` だけラフにする。芝の中の池でも水際から直接打たせない
- 池とOBには高さの濃淡を掛けず単色。高さを読む対象を芝だけにする
- 池は見た目だけ `0.12m` 下げて一段低く見せる。ハイトマップと物理は変えない
- 木をコース枠の外の環状から**枠内のOBエリア**へ移し、地面の高さに立たせる
  - OBの位置そのものが木で分かる
  - サーフェス分類のない検証ページ（`/green-test/`）では従来どおり枠の外へ並べる

数値はいずれも暫定。実機確認の結果で詰める。

### T3.5の実機確認2回目を受けた表示修正

**実装済み・確認待ち。** 方向調整（ADDRESS AIM）画面の表示不具合を2件直した。

#### ボールが画面外に落ちる

長方形コースでホールが長くなり、注視点だけで見下ろし角を決める `addressPose` が破綻していた。
390×844の縦画面・試験ホールのティー（カップまで13.6m）で、ボールの接地点は画面Y **891px / 844px** ＝ 画面外。

- 対策は「注視点距離に上限を置く」ではなく、**ボールが画面内に残るピッチを幾何から解く**方式を採った
  - 上限値は画角・立ち位置・目線の高さが変わるたびに合わなくなる。要求そのものを式にしたほうが壊れない
  - ボールの接地点が画面高さの `address.ballScreenMaxFraction`（0.86）より下へ行くときだけ、足りない分だけ視線を下げる
- 結果、ボールの接地点は距離によらず **726px** に収まり、カップ（303px）と旗竿の先端（240px）も画面内に残る
- ボール位置15×45点 × 狙い72方向 ＝ 93,960通りで、最下点は **957.5px（修正前）→ 725.8px（修正後）**
- LOW_LINEは高さ0.4m・距離1mで、ボールが視点の約22°下（縦の半画角35°以内）にあり構造的に外れない
  - 同じ総当たりで修正前後とも 717.9px。**同じ問題は起きていないので変更していない**

#### ガイドがボールの手前に描かれる

ガイドはWebGLキャンバスの上の2D Canvasに描いていて深度を持てず、常に最前面に出ていた。

- 検討した案
  - (a) 3Dの線に深度テストを戻す：正式採用の「滑らかな半透明ライン」の見た目を失う
  - (b) ガイドを3Dの半透明メッシュにする：1/3解像度のドット処理を通るので、やはり滑らかさを失う
  - (c) **画面上のボールの円をクリップから抜く（採用）**：見た目はそのままで前後関係だけ作れる
- 球のシルエットは円なので、ボールの実寸から求めた半径（`aim.guideBallMaskScale` 倍）で正確に隠せる
- ガイドはボールより奥（ADDRESSで0.028m、LOW_LINEで0.033m、STROKEカップ確認で0.008m）にあり、
  遮蔽の向きは常にこの一方向だけでよい
- 内部に残してあるドット版（DOTモード）の3D線も `depthTest` を戻し、同じくボールに隠れるようにした

ガイドは従来どおり約0.5mの直線のまま。曲がり予測は出さない。

### STROKEカップ確認でガイドが見えない件

**実装済み・確認待ち。** 上記の修正後の実機確認で、カップ確認画面にガイドが出ていないと分かった。

原因は幾何。この視点はボール真上1.5mから前を見るので、ボール直後のガイドは視線の
**70〜89度下**にあり、縦の半画角35度にどうやっても入らない（ボール自体も画面外にある）。

| カップまで | 見下ろし角 | ガイド手前端 | ガイド遠端 |
| --- | --- | --- | --- |
| 13.6m | 6.5度 | 4868px | 1628px |
| 4.5m | 16.1度 | 2352px | 1246px |
| 2m | 33.9度 | 1279px | 861px |

- 案A（視線を下げて入れる）は却下。ガイド遠端を入れるには見下ろし37.7〜43.2度が必要で、
  カップが58px（HUDの下）、旗竿の先端は画面外になる。しかもガイド手前端は結局入らない
- **案B（採用）**: 線の長さと向きは変えず、**置く位置だけ狙い方向へ進める**
  - 手前端が画面高さの `stroke.cupCheckGuideScreenFraction`（0.92）へ来る距離を見下ろし角から解く
  - 実測でボールから 0.65〜1.98m 先、画面Y 743〜779px。ホールの長さによらず同じ高さに出る
  - カメラは変えていないので、カップ（423px）と旗竿の見え方は従来どおり
  - ボール位置15×45点 × 狙い72方向 ＝ 93,960通りで常に画面内（手前端の最下点 836.9px）
- 引き換えに、この視点だけ線がボールから離れて浮く。ボールが写っていない視点なので見た目には出ない

計測中に気づいた別件（今回は変えていない）: カップまで0.5m以下だと `lookDistanceMin` 2m の
注視点が実際のカップより奥になり、カップ確認でカップが画面下（855px）に外れる。

### T3.6. コースマップ

**実装済み・実機確認待ち。** 実機で「打つ前にどこへ打てるのかの全体像が分からない」と指摘されたため、
コース全体を真上から見渡す視点を追加した。

- ADDRESSの視点ボタンに5つ目として **マップ** を追加（方向調整 / 低い視点 / カップ後方 / 横から / マップ）
  - 独立した入口を新設せず既存の並びに足した。ラベル4〜5字なら幅375pxでも1行に収まる
  - ADDRESSは定義上ボールが止まっている状態なので、「走行中に俯瞰へ切り替えない」原則と衝突しない
- カメラは `RESULT` の真上俯瞰と同じ作りで、**枠取りだけ `course.bounds` に変えたもの**
  - 見下ろし角 89.9°、FOV 55°、余裕 1.08倍、向きは -Z（RESULTの yaw 0° と同じ）
  - 視距離は「横（X）が収まる距離」と「縦（Z）が収まる距離」の遠いほう
  - 縦画面は横の画角が狭いので、幅の広いコースでは横が決め手になる
  - 16m × 24m の試験ホールでの計算値

    | 画面 | アスペクト | 必要距離（縦 / 横） | 採用距離 | 画面に入る範囲 |
    | --- | --- | --- | --- | --- |
    | 375×667 | 0.562 | 24.9 / 29.5m | 29.5m | X±8.64m ・ Z±15.37m |
    | 390×844 | 0.462 | 24.9 / 35.9m | 35.9m | X±8.64m ・ Z±18.70m |
    | 412×915 | 0.450 | 24.9 / 36.9m | 36.9m | X±8.64m ・ Z±19.19m |
    | 810×1080 | 0.750 | 24.9 / 22.1m | 24.9m | X±9.72m ・ Z±12.96m |

    必要な半幅は X 8.0m / Z 12.0m なので、いずれも枠の外まで入る。
    細い縦画面では横が、iPad比率では縦が決め手になり、どちらの側でも余裕は 1.08倍を下回らない
- ボール（白）とカップ（赤）の位置を、**斜めの矢印とドット文字のラベル**で示す（`src/course-map-marker.ts`）
  - 実寸のボールは真上30m超では見えないため
  - ラベルはボールが**ティー上なら `TEE`、それ以外は `BALL`**、カップは `CUP`
  - 3×5の大文字ドットフォントを自前で持つ。曲線がないのでドット絵の画面と揃う
  - 1アートピクセル＝画面のドット2個ぶん。390×844で 39〜51 × 45px
  - 塗りの周り1ドットを暗く縁取り、芝の緑にも池の青にも埋もれないようにする
  - アートは点の左上へ伸びる。コースの左半分の点だけ左右を入れ替える（文字は鏡像にしない）
  - スプライトは `sizeAttenuation` なし。視距離が変わっても画面上の大きさとドットの粒を保つ
  - マーカーはマップの間だけ表示する
  - 検算（390×844・dpr2・視距離35.9m）: 矢印の先端がボール・カップの投影点とぴったり一致し、
    枠の四隅にボールがある場合でもアートは画面内に収まる
- **曲がりの予測線・推奨ルート・長いラインは描かない。** サーフェスの塗り分けと2点の位置だけ
- マップ表示中に画面サイズが変わったら枠取りを取り直す

---

## T3.8. コース境界の自然な揺らぎと検証器

**実装済み・実機確認待ち。** 実機確認で「コース生成としてはだいたいいいが、形が幾何学模様すぎる」と
指摘された分。素の定義のままだと、芝は折れ線を一定幅で太らせた帯、池は真楕円、岸は均一な輪になる。

### 変更

- 芝・ラフ・セカンドカットの幅を、位置ごとの倍率で増減させた（`src/course/course-noise.ts`）
  - 2DバリューノイズのfBm。ルートに沿った位置だけでなく左右でも変わる
  - 折れ線からの距離だけで揺らがせると、左右対称の「太らせた折れ線」のまま残るため
  - 帯ごとに別のノイズ列を引き、3本の縁が平行にならないようにした
- 池の輪郭を、正規化した楕円座標の半径を角度ごとに歪めた形にした
  - 波数2〜5の余弦波の重ね合わせ。角度の関数なので輪郭が必ず閉じる
- 岸（`waterFringe`）の幅も角度ごとに変えた。広い浅瀬と切り立った岸が交互に出る
- 揺らぎの値は全て `CONFIG.course.edgeNoise` に置いた
- シードがコースの外形も決めるようにした（従来はうねりだけ）
  - `?seed=12345`、または `?debug=1` のときだけ出る「シード ⟳」で切り替えると外形ごと別のホールになる
  - 実機で複数のシードを見比べて、揺らぎの効き方を確かめるため

### 決定論の守り方

デイリーチャレンジは全員が同じコースを遊ぶので、形は `CourseDefinition.seed` だけで決まる必要がある。

- ノイズは**列（mulberry32のような擬似乱数列）ではなく格子点ハッシュ**から1点ずつ引く
  - `surfaceAt(course, x, z)` が座標だけで決まる純関数のままになり、呼ぶ順序・回数に依存しない
  - 物理は毎ステップこれを呼ぶので、この性質は外せない
- 池の輪郭の調和成分だけはコース定義ごとに一度作って持つが、中身はシードとハザードの並び順から
  決まる派生値なので、キャッシュの有無で結果は変わらない
- 揺らぎの振幅は必ず100%未満。ノイズが [-1, 1] に収まるので帯の幅が0以下にならず、
  **芝が途切れてOBで分断されることが構造的に起きない**
- ティーとカップの周囲 `safeRadius`（0.7m）は最後に必ず通常芝で上書きする

### 検証器

`src/course/course-validate.ts` を追加し、`npm run validate:course` で回せるようにした。

- `validateCourse()`: 0.1m格子で走査し、面積比・ティーからカップまでの芝の連結（4近傍BFS）・
  ティー/カップが通常芝かを返す
- `courseSurfaceDigest()`: 同じ格子のサーフェスを1つのハッシュにまとめる。
  同じシードで作り直した定義と突き合わせて決定論を確認する
- Node から `src` のTypeScriptをそのまま実行するため、拡張子を補う解決フックだけ
  `scripts/ts-resolve*.mjs` に置いた。依存パッケージは増やしていない

試験ホールの実測（決定論OK・ティー→カップ連結OK）:

| | 通常芝 | ラフ | セカンドカット | 池 | OB |
| --- | --- | --- | --- | --- | --- |
| 揺らぎなし | 21.3% | 18.3% | 23.8% | 5.3% | 31.2% |
| 揺らぎあり | 21.4% | 21.1% | 23.3% | 5.4% | 28.9% |

シードを200通り掃引しても（格子0.15m）、全てのシードでティー・カップは通常芝、
ティーからカップまで芝が繋がる。OB面積比は最小28.5% / 平均31.5% / 最大34.8%。

波長5.5m・振幅0.3では縁がほぼ直線に見えたので、波長4.2m・振幅0.4〜0.5にした。
見た目の自然さの合否は実機確認で判断する。

---

## T4. コース生成器と地形の性格

シードひとつから遊べる1ホールを作れるようにした。T3.8 で先に用意した検証器
（`validateCourse`）が、ここで初めて「生成の合否を決める側」として使われる。

### 地形の性格（`CourseDefinition.terrain`）

ランダムなうねりだけでは、どう転がるかを読む対象にならない。
読める形を5種類そろえ、そこへうねりを足す形にした。

- `random` … 従来どおりの緩い全体傾斜＋うねり
- `singleSlope` … 片流れ
- `receiving` … 受けグリーン
- `saddle` … ポテトチップ
- `twoTier` … 2段グリーン

`receiving` / `saddle` / `twoTier` はカップを原点・最終アプローチの向きを u 軸とする
局所座標で組み立てる。ドッグレッグでも向きが意図どおりになる。
u と v は tanh やスムーズステップで潰すので、ホールが長くても高さは振幅内に収まる。

数値は `CONFIG.course.terrain`。5シードの実測は `docs/spec.md` §1「地形の性格」の表。
2段の段の面だけ約21%と急だが、上る高さは0.24mなので初速があれば上がる。

**`terrain` を指定しない場合（`random`）の高さは、変更前と完全に一致する。**
性格用の乱数はうねりのガウシアンを引き終わってから引くようにしたので、
既存のうねりの並びが1つもずれない（4シード・約4万点で最大差 0.000m を実測）。
`/green-test/` の見た目も変わらない。

### 生成器（`generateCourse`）

難易度（やさしい/ふつう/むずかしい）と型（ストレート/ドッグレッグ/S字）を重みで選び、
ルートを折り曲げ、コース枠を決め、池を置き、地形の性格を選び、**検証器へ通す**。
通らなければ枝番を進めて作り直すので、同じシードなら必ず同じホールになる。

- コース枠は「揺らぎが最大に振れたときの芝の幅 × 係数」でルートの外接矩形から広げる。
  当初は揺らぎなしの幅を基準にしていたが、それだと盤面が丸ごと芝に収まって
  OBが消えるストレートばかり弾かれた（200シード中11本しか通らなかった）
- 受け入れ条件は検証器の合格 ＋ OB面積比 18〜45%
- par はルート全長から決める。打ち切りは従来どおり `par + 5`
- 数値は `CONFIG.course.generator`

`npm run validate:course` に生成器の200シード掃引を足した。決定論・連結・面積比・
par と地形の内訳をまとめて出す。実測は `docs/spec.md` §1「コース生成器」。

### ゲーム本体への接続

- シードを切り替えると**ホールごと別のもの**になる（従来は試験ホールの縁とうねりだけ）
- `?course=prototype` で従来の手作り試験ホールに戻せる
- 打ち切り打数はコースごとに par が変わるので、その都度求めるようにした
- `?debug=1` のときだけ、状態表示の横に地形の性格を出す

---

## T7. 複数ホール進行

ホール単位だった作りを、**ホールを順に回るラウンド単位**にした。
固定9ホールの中身を選ぶのは次のタスクで、ここは進行と表示の器を作る。

### ラウンドの状態（`src/round.ts`）

`Round` クラスがシードの配列と何ホール目かを持ち、ホールアウトのたびにスコアを積む。
**数の管理だけ**を持ち、コースの生成も表示も知らないので、
通常ツアー（固定9ホール）にもデイリー（自動生成3ホール）にも同じものを使える。

- `holeNumber` / `holeCount` / `currentSeed` / `hasNext`
- `recordHole(par, strokes, holedOut)` … 1ホールにつき一度だけ積む（二重記録は無視）
- `totalStrokes` / `totalPar` / `toPar` … ホールアウト済みのぶんだけ
- `next()` / `reset()`
- `formatToPar()` … `±0` / `+2` / `-1` の表示

### コースデータ（`src/course/tour-holes.ts`）

正本は**セット名とシードの配列**。ホールの中身はシードから `generateCourse` が決めるので、
選び直すときはここの数字を差し替えるだけでよい。

T9aで1000シードを調査した後、試遊用に3セット・全27ホールを置いた。

- 風の丘（`?tour=breeze`）: 池と大きな曲がりが少ない
- 曲がりの森（`?tour=forest`）: ドッグレッグ・S字と遠回り率を重視
- 水鏡の庭（`?tour=waterside`）: 池の数と水面積比を重視

各セットはパー3×2・パー4×5・パー5×2（合計パー36）。
パー4はすべて「むずかしい」で5種類の地形を1本ずつ使い、27シードは重複しない。
**機械指標で選んだ試遊版であり、面白さとテーマ名は実機確認後に確定する。**

### 画面遷移

```text
… → RESULT →（ホールアウトなら）HOLE_OUT → 次のホールの ADDRESS
                                        └（最終ホールなら）ROUND_END
```

- `HOLE_OUT` … RESULT の俯瞰と軌跡を**背景に残したまま**スコアカードを重ねる。
  最後の一打の軌跡を見てからカードが出るように `CONFIG.game.round.cardDelay` だけ待つ
- カードは指を通す（`pointer-events: none`）ので、「進む＝タップ」の規則をそのまま使える。
  待ちが明ける前のタップはカードの早出しにする（打ち直しにはしない）
- `ROUND_END` … 全ホールの一覧表と合計。ここだけは「もう一度」ボタンで進む。
  誤タップでラウンドをやり直させない

### プレイ中の表示

`HOLE 3/9 ・ PAR 3 ・ 2打 ・ 合計 +2` の1行を常時出す。
`合計` はホールアウト済みのぶんのパー差で、プレイ中のホールは `打` の側に出る。

### 練習モードの残し方

今までの「同じホールを打ち直す」挙動は**練習モード**として残した。既定は通常ツアー。

- `?mode=practice` … 練習。`?mode=tour` で明示的にツアー
- `?seed=` と `?course=prototype` は1ホールを繰り返し試すための指定なので、自動で練習になる
- シード⟳ボタンは練習モードだけ。ツアー中にホールを差し替えない
- ツアー中は URL に `?seed=` を書かない（読み込み直しで練習モードになってしまう）

### まだやっていない

- 試遊用3セットの実機確認と、違和感があるホール・テーマ名の差し替え
- デイリー、ランキング、自己ベストの保存、広告

---

## T7.1. ラウンドの進行の保存と再開

T7 の実機確認で「iPhoneでホーム画面に戻ると、戻ったときにラウンドが最初からになる」と
分かった。バグではなく未実装で、`Round` はホール番号もスコアもメモリ上だけに持っていた。
iOS Safari はバックグラウンドのタブを捨てて復帰時に読み込み直すので、そこで消えていた。

### 方針

**現在のホールの頭から再開する。** 進行中のホールのボール位置・打数・狙いは持たない。
そこまで保存すると狙い・直前のショット位置・罰打の有無まで整合を取る必要があり、
保存漏れがそのままバグになる。仕様は `docs/spec.md` §6。

### 置き場所

後のデイリー（日付ごとの進行）と自己ベストでも使い回せるように、保存を一箇所へまとめた。

- `src/round-storage.ts` … `RoundProgressStore`（localStorage への出し入れ）と
  `seedsId()`（シードの並びから決まる識別子）。ラウンドの中身は知らない
- `Round.snapshot()` / `Round.restore()` … 進行の取り出しと戻し。
  戻せたときだけ `true` を返し、少しでも合わなければ戻さない
- 保存キーとバージョンは `CONFIG.game.round.save`

### 古い保存の扱い

固定9ホールのシードは次のタスクで選び直す。**並びが変わったのに古い保存を読むと、
ホール番号と中身がずれて別のホールのスコアが混ざる。**
保存に形式のバージョンとシード列の識別子を入れ、バージョン違い・シード列違い・
壊れた JSON は、どれも黙って捨てて最初から始める。

### 保存と復元のきっかけ

- 保存 … ホールアウトしてスコアが確定したときと、次のホールを読み込んだとき。1打ごとには保存しない
- 片付け … ラウンド終了時と、ROUND_END の「もう一度」。最終ホールのホールアウトでも片付ける
  （再開できるホールがもうない）
- 復元 … 読み込み時に一度だけ。**通常ツアーのときだけ**で、練習モードは保存も復元もしない
- ホールアウトのカードを出している間の保存は、記録済みのぶんだけ先へ進めた形にする。
  カードは1タップで消えるだけの表示なので、再開先は次のホールの頭でよい

### 復帰したときの知らせ

復帰後の最初の ADDRESS で `ホール3から再開します` を通知行に出し、最初の操作で通常の案内へ戻す
（進行中のホールの打数は失われるので、黙って戻すと打数が減ったように見える）。
カードで確認させる案もあったが、iPhone はホーム画面の往復が多く、そのたびに1タップ増えるので採らない。

### 自動確認したこと

保存を入れて読み込み直すとホール番号・ホールアウト済みスコア・合計とパー差が戻る／
9ホール通しで各ホールアウトの保存が積まれ、ROUND_END で片付く／
`tour-holes.ts` のシードを1つ書き換えると古い保存が捨てられる／壊れた JSON も同じ／
練習モード（`?seed=` / `?course=prototype`）は保存を読まず書かない。

**実機確認OK。** iPhoneでホーム画面へ戻って戻る操作で、そのホールの頭から再開できている。

あわせて、T7の実機確認で挙がっていた「カメラ遷移が激しい」は、この時点で
**気にならなくなったため取り下げ**（どの変更で解消したかは特定していない）。

---

## T7.2. コースマップの向きとカップの旗

**実装済み・実機確認待ち。** T7の実機確認で出た4件の指摘のうち、マップに関する2件を直した。
ラウンドの保存は T7.1、カメラ遷移はまだ手つかず。

### 向きが上下逆だった

ふつうのゴルフのコース図は上がピン・下がティー。実装は逆だった。

- `courseMapPose` はワールド固定で -Z を画面上に向けるだけだった。
  一方 T4 の生成器はルートを z:0 → z:total で作るので、生成コースはティーが -Z（画面上）になる
- 手作りの試験ホールだけ `tee.z 8 / cup.z -3` で意図どおりだったため、
  T4 で生成コースが既定になったときに逆転したまま気づかれずに来た
- 実測（ツアー9ホールを実際に生成）: 9本とも `tee.z < 0 < cup.z` で例外なく上下逆

**採ったのは「ホールごとに向きを決める（軸固定）」案。**

- `courseMapRotated(tee, cup)` が `cup.z > tee.z` のときだけ真を返し、カメラを枠の -Z 側へ置く
- 回すのはワールドの ±Z のどちらを画面上にするかだけ。斜めには回さないので、
  コース枠は画面に対して軸平行のまま＝枠取りの計算をそのまま使える
- 基準は tee → cup なので、ホールの途中でボールが動いても向きは変わらない
- 180°回したホールでは +X が画面の左になる。マーカーの左右反転の判定も
  ワールドの X ではなく**画面の左右**（`courseMapScreenXSign`）で行う
- RESULT の真上俯瞰（`yawDeg: 0`）は**触っていない**。
  あちらは「打ち出し → カップの向きが画面上」で、結果としてカップは上に来る。
  上方向がワールド軸か一打ごとかの違いは残す

### マーカーの絵

「ピンは旗の絵のほうが分かりやすい」という指摘に対応した。

- カップのマーカーを**旗の絵**にした。`CUP` の文字は落とした
  - 旗竿の根元が指し示す点。根元に左右対称の足（3アートピクセル）を付け、
    どの点を指しているかを矢印と同じ精度で読めるようにした
  - 旗は**右へたなびく四角い旗で固定**（5 × 4アートピクセル）。左右は反転しない
  - 旗竿はゲーム内の旗竿と同じ色。`0xf0f0f0` が `green.ts` に直書きだったので
    `CONFIG.hole.flagstickColor` へ出し、ゲーム本体とマーカーで同じ値を見るようにした
  - 旗竿の高さ12アートピクセル。390×844・dpr2 で 27 × 42px
- **ボール側の矢印とドット文字は変更なし**（3×5フォント・矢印7・粒2ドット・縁取り1）
  - 一度は粒を半分にして5×7フォントへ作り直したが、文字の見かけの大きさを保てないため戻した

### 検算（390×844・dpr2・実際に投影して確認）

- 375×667 / 390×844 / 412×915 の3画面 × ツアー9ホール＋試験ホールの30通りで、
  **30通りとも画面上のカップがティーより上**（実際に `courseMapPose` の姿勢でカメラを組み、
  three.js の投影で画面座標を出して確認）
- 旗のはみ出しは30通りとも0px
- ボールのマーカーは、コース全体を0.25m刻みで走査し
  **ボールが実際に止まりうる面（芝・ラフ・セカンドカット）**すべてで、
  30通り中29通りがはみ出し0px。375×667のシード128だけ、カップの5m先の
  セカンドカットで上端が1.1pxはみ出す（同じ走査を回す前の向きで行うと10.7px なので、悪化ではなく改善）
- 枠の四隅（＝深いOB。ボールは止まれないので実際には起こらない）は最大15.5px（375×667）

---

## T8. カップまわりの物理（旗竿の当たり判定と、縁の鏡面反射の削除）

**実装済み・実機確認OK（PR #44）。** 実機で「カップに弾かれたときの跳ね返りが強すぎる」と分かった件。
初期値のまま実機で問題なしだったので、`CONFIG.physics` の6つは振り直していない。

### 何が起きていたか

`checkCup()` は、カップ中心 54mm 以内で速度が 1.4m/s 以上のとき、
**カップの縁の法線で速度ベクトルを完全に鏡面反射**させ、法線方向も接線方向もまとめて
`lipOutDamping` 0.7 倍にしていた。実際のパットで向こう縁に当たって軌道が反転することはまず起きない。

このゲームは**ピンが挿さったまま**の前提なので、ボールは向こう縁に届く前に旗竿へ当たる。
跳ね返って見えていたものの正体は、本来は旗竿への衝突だった。
旗竿の当たり判定は存在せず、`flagstick*` は描画用の値だけだった。

### 入れたもの

- **縁の鏡面反射を削除**。`lipOutDamping` は役目がなくなったので消し、今回の係数へ置き換えた
- **旗竿の当たり判定**。XZ平面の円柱として、ボール中心が竿の中心から
  `flagstickRadius 12mm + ball.radius 21.3mm = 33.3mm` に達したら衝突。
  1ステップ 1/240 秒でも速い球は 2cm 進むので、位置のサンプルではなく**線分と円の交差**で見る。
  法線（竿の中心からボールの中心へ向く向き）成分は反転させて `flagstickRestitution` 0.25 倍、
  接線成分は `flagstickTangentKeep` 0.8 倍。
  衝突後の速度が `flagstickCaptureSpeed` 0.6m/s 未満なら口の中なのでカップイン
- **捕まえられる速度の上限を横ずれの関数にした**。口を横切る弦の長さに比例させる
  （`cupCaptureSpeed` 1.4m/s × 弦の比 ^ `cupCaptureFalloff` 1.0）。
  固定 1.4m/s の崖をやめたので、「芯を通れば強くても入る／縁をなめると弱くても外れる」が
  特別扱いなしに出る
- **口を横切って外れる場合は軌道を反転させない。** `cupGrazeSpeedLoss` 0.25 まで減速し、
  `cupGrazeTurn` 0.3rad までカップ側へ向きを変えて、そのまま先へ流れる
- 口の中で停止したらカップイン（竿に当たって力尽きた場合を含む）
- 数値は全て `CONFIG.physics`。初期値は提案であって、**良し悪しは実機で決める**

曲がりと減速は**口へ入るときではなく出るときに一度だけ**与えている。
横切る途中で向きを変えると、竿に当たらないはずの横ずれ（33.3〜54mm）から竿へ入ってしまい、
2つの領域の切り分けが崩れるため。

### 検算（平坦・スティンプ10ft・カップ到達速度で整理）

入る到達速度の上限:

| 横ずれ | 0mm | 5mm | 10mm | 20mm | 30mm | 33mm | 34mm | 36mm | 40mm | 45mm | 50mm | 53mm |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 上限 [m/s] | 2.41 | 2.19 | 1.78 | 1.30 | 1.17 | 1.11 | 1.09 | 1.05 | 0.95 | 0.79 | 0.57 | 0.35 |
| 上限より速いと | 竿弾 | 竿弾 | 竿弾 | 竿弾 | 竿弾 | 竿弾 | なめ外 | なめ外 | なめ外 | なめ外 | なめ外 | なめ外 |

33.3mm を境に「竿に弾かれる」と「なめて外れる」が入れ替わる。設計どおり。
横ずれが小さいほど上限が高いのは、旗竿が受け止めるぶん（芯なら 2.41m/s まで入る）。

- **当たった側へ出るか**: 到達2.5m/s で横ずれ +20mm → 停止 x=+1.501 / −20mm → x=−1.501。
  ±30mm では x=±1.717。左右対称で、**右に外して当たれば右、左なら左**へ出ている
- **軌道の反転**: 横ずれ0〜53mm × 到達0.1〜4.0m/s の10530通りで、
  **竿に触れずに反転した数は0**。竿に当たって反転したものは1747（正面衝突なので正しい）
- **なめて外れる軌跡**（到達1.5m/s・横ずれ −45mm）: x=−0.045 で口を横切り、
  出たあとカップ側へ曲がって x=+0.195, z=−1.466 で停止。反転なし
- **短いパット（残り0.5m）で入る初速の幅**（変更前は横ずれによらず 0.700〜1.565m/s）
  - 横ずれ0mm: 0.705〜**2.505**（旗竿が受け止めるので**広がった**）
  - 10mm: 0.705〜1.910 / 20mm: 0.705〜1.475 / 30mm: 0.710〜1.360
  - 40mm: 0.720〜1.180 / 50mm: 0.730〜0.895（縁寄りは狭くなった＝狙いのズレが結果に出る）
- **決定論**: 同じ初速・同じ方向で2回打って、軌跡の点が完全一致することを
  竿入・竿弾・なめ外の各ケースで確認
- `npm run build` と `npm run validate:course` は通る

### 今回やらないこと

「なめて入る」を円を描いて落ちるように見せる**演出は入れていない**。今回は物理だけ。
実機確認で問題なしだったので、**演出は当面入れない**。また気になったら挙げ直す。

---

## ゲーム全体の仕様検討

**基本方針は確定済み。** 詳細は `docs/spec.md` §6。

- 打数を競うストロークプレー
- コンセプト別の固定9ホール通常ツアー
- 全員共通の自動生成3ホール・挑戦無制限のデイリーチャレンジ
- 通常ツアーとデイリーの自己ベスト・オンラインランキング
- ランキング対象外の練習モード
- 3ホール終了時、および途中離脱から別ゲーム開始時の全画面広告

次の実装順は、試験ホールの実機確認後に以下を基本とする。

1. ~~コース生成器と検証器~~（T3.8 の検証器と T4 の生成器で完了）
2. 複数ホール進行と9ホール固定コースデータ（3セットの試遊版まで完了、最終確認待ち）
3. デイリーのseed確定・保存・再現
4. スコア保存とランキング
5. 広告を含むゲーム開始・離脱・完了遷移

---

## T3.7. 操作の一貫性とUI配置の整理

**実装済み・実機確認待ち。** 実機確認で「ボタンがホールと重なる」「STROKEへ行くのがボタンだったりタップだったりする」と
指摘され、遷移と配置の規則を決め直した。

### 決めた規則

- **進む＝画面タップ / 切り替え・戻る＝ボタン**
- 左右スワイプは狙い角の調整専用

### 変更

- 読み4視点すべてでタップからSTROKEへ入れるようにした
  - 以前は方向調整・低い視点の2つだけ。カップ後方・横から・マップではタップが効かず行き止まりに見えていた
- マップを視点バーから外し、右下の単独ボタンにした
  - 視点ボタンは 353px → **290px**。360px幅の端末で2段に折り返さなくなった
  - マップはタップで閉じ、開く前の読み視点へ戻る
- 開発用の表示切替（シード / ドット / ライン / 地形）を `?debug=1` のときだけ表示にした
  - パター反発の「調整」は練習用の本番UIなので残す

### STROKE中のボタン配置

実機で「残り50cmのショートパットでボタンとホールが重なる」と指摘された分。

- 真下視点は仕様上**画面左方向＝狙った方向**なので、カップは左へ出る
  - 出るのは残りが視野半幅（約0.485m）未満のときだけ。390×844の残り0.3mでカップ口は (74, 422)
  - 「カップ確認」列は左中央（x 8〜98 / y 383〜461）にあり、**直接重なっていた**
- 「方向調整に戻る」は下中央にあり、**つま先の描画**（y 796〜826 / x 108〜282）と重なっていた
- 両方とも画面上部の右へ縦に積み直した。上部はこの視点で唯一空いている領域
  - HUDの右パディングを116pxにしてボタン列と干渉しないようにした
  - 調整パネルの開く位置をボタン列の下（top 104px）へ下げた

### 用語とボタン配置の見直し（実機確認2回目）

- **つま先の絵を削除。** 実機判断で不採用
- STROKE中のボタンを画面上部から**下の左右の角**へ移した
  - 右下＝「読みに戻る」／左下＝「狙いを見る」「手元を見る」
  - つま先が無くなり下端が空いたため、親指の届く角へ戻せるようになった
- 用語の付け方を決めた
  - **視点ボタンはカメラの位置を名乗る。** 役割では名乗らない
  - **動作ボタンは動詞で名乗る**
  - 同じものに複数の名前を付けない

| 場所 | 旧 | 新 | 理由 |
|---|---|---|---|
| 視点 | 方向調整 | ボール後方 | 狙いは低い視点でも変えられるので役割名として不正確 |
| マップ | マップ | コースマップ | HUD表示と不一致だった |
| STROKE左下 | カップ確認 | 狙いを見る | カップへ向くとは限らない |
| STROKE左下 | ストロークに戻る | 手元を見る | 「ストローク」は打つ行為で視点名ではない |
| STROKE右下 | 方向調整に戻る | 読みに戻る | 戻り先は特定視点ではない |
| 案内文 | タップでストローク | タップで構える | タップは打つ操作ではない |
| 案内文 | タップで次のパット | タップで次の一打 | 打数の「打」と揃える |
| 調整入口 | 調整 1.00× | 強さ 1.00× | 何の調整か分からない |
| パネル題 | ストローク調整 | パターの強さ | 同じものに3つ名前があった |
| パネル内 | パター反発 | 強さ | 同上 |

HUDの状態名（ADDRESS / STROKE / …）は内部名なので `?debug=1` のときだけ出すようにした。

### 「狙いを見る」のガイドを画面下端の外まで伸ばす

実機で「黄色い棒が浮いているように見えてガイドと分からない」と指摘された分。

- 実測すると、ガイドは画面の 92%→71% あたりに収まっていて、下端まで8〜12%の隙間があった
- 手前端を画面下端の外（102〜110%）へ出し、足元から前へ伸びる線として見せる
- **奥端は動かさない。** 前方に見える範囲が変わらないので曲がりの予測は増えない
- 引き換えにこの視点だけ線の世界長さが 0.5m → 0.73〜0.97m になる

### まだ手を付けていない

- ドラッグ（狙い調整）とタップ（打つ）が同じ面を共有している点。誤爆の可能性は残る
- 現在どの視点かがボタンの色でしか分からない点

---

## T6. 調整とデバッグ表示（未着手）

**既存プロトタイプの調整タスク。**（生成器を T4 に置いたので、この番号を T6 へ送った）

`docs/spec.md` §5を基準に進める。

主な内容:

- デバッグオーバーレイの整理
- `lil-gui` の調整項目を現在の実装に合わせて整理
- 指がボールを隠す問題の対策比較
  - (a) ボールを大きめにする
  - (b) 周囲にリングを出す
  - (c) タッチ位置を手前へオフセット
- 必要なら開発用表示を追加するが、本番UXへ持ち込むものは確認を取る

ここからは実機で触りながら数値を詰める。
ゲーム仕様・UXを変える選択は、選択肢と推奨案を出して確認してから実装する。

---

## T5以降

T3.5の実機確認は完了。コース生成器は T4、複数ホール進行は T7 で実装済み。
ゲームモードの実装順は上記「ゲーム全体の仕様検討」に従う。

見た目の仕上げやサウンドなどの演出は、コース生成・複数ホール進行・ランキング・広告の基盤より後に扱う。

RESULTの軌跡表示はすでに実装済みなので、旧T5候補の「軌跡を芝に残す」は完了扱い。

---

# 進め方のルール

## 実装単位を分ける

チャットを継続する場合も、レビューと実機確認ができる大きさにブランチ・PRの責務を分ける。
文脈は `CLAUDE.md`、`docs/spec.md`、この `TASKS.md` に残す。

## 仕様変更は記録も同期する

ゲーム仕様・UXを変更したら、実装確定後に `docs/spec.md` と必要な記録を現在状態へ合わせる。
比較中の機能は正式採用と混同しないよう「比較用」「暫定」と明記する。
チャットだけに決定事項を残さない。

## 実機確認はユーザーが行う

操作感・見た目に関わる変更について、AIは「良くなった」と自己判断しない。
GitHub Pagesへ反映後、「実機で確認してください」と明示する。

## 詰まったら症状を具体化する

感覚だけでなく、可能なら状態・数値・再現手順を残す。
デバッグ表示はそのために使う。
