# タスク分割 / 現在状況

**1タスク = 1セッション。** 終わったらセッションを閉じて、次は新しいセッションで始める。
仕様の正本は `docs/spec.md`。恒久的な前提は `CLAUDE.md`。

このファイルは、実装済みの履歴と「次に何をするか」を残す。
古い試行錯誤より**現在の実装を優先**する。

---

## T0. 土台と自動デプロイ

**完了済み。**

- Vite + TypeScript + three.js
- GitHub Pagesへ `main` push時に自動デプロイ
- 現在は**スマホ縦画面専用**
- 横画面では「縦にしてください」を表示

ゲーム本体:
`https://hanagemaru.github.io/putt/`

---

## T1. スワイプ計測の単体ページ

**完了済み。** `/swipe-test/` で実機確認済み。

- `getCoalescedEvents()` を含むポインタサンプルを記録
- インパクト直前40msを最小二乗フィットして速度算出
- 判定ラインとの交差時刻を線形補間
- 直近10回の履歴・ばらつきを表示
- バックスイングなしでは打てないゲートを実装
- 現在の `minBackswingPx = 20`

旧記録の40pxは現在値ではない。

スワイプ計測:
`https://hanagemaru.github.io/putt/swipe-test/`

---

## T1.5. パターの所作

**完了済み。** 実機確認済み。

- フェースがボール右端に触れた位置をインパクトとする
- 指のY変位で芯からのズレを表現
- 芯を外すと初速を減衰、フェース端を超えると空振り
- インパクト後もパターは指に追従
- ボールを追い越さないよう位置を頭打ち
- `/swipe-test/` の待機位置は `putterRestOffsetPx = 36`
  - ボール半径28px + パター半幅5px + 隙間3px
- ゲーム本体では3Dボールの見かけ半径に合わせ、**ボール表面から約3px後ろ**へ待機させる

現在の主要値:

- `putterLength = 64`
- `putterWidth = 10`
- `sweetSpotPx = 12`
- `mishitMinGain = 0.55`
- `faceMaxAngleDeg = 35`
- `speedK = 0.00266`
- `directionSensitivity = 0.4`

`/swipe-test/` のボール転がりは入力確認用の演出で、ゲーム本体の物理とは別物。

---

## T2. グリーンと物理

**完了済み。**

完了条件:

- 傾斜で曲がる
- スティンプを変えると転がる距離が変わる
- 上りで止まり、勾配が摩擦を上回る下りでは止まらない

### 現在の濃淡方針

**高さベースが正式採用。**

- 明るい = 高い、暗い = 低い
- グリーンごとの最小最大で正規化しない絶対スケール
- `gradientStrength = 0`
- `heightStrength = 0.75`
- `heightRange = 0.25`
- 勾配ベースの表示は比較用コードとして残すがゲーム本体では使わない

過去に勾配ベースも試したが、明るさが高さを意味しなくなり、実機で曲がる方向の読み違いが起きたため現在は採用していない。

### 現在のドット表現

- `pixel.scale = 3`
- `pixel.levels = 24`
- 1段 ≒ 0.7cm
- `pixel.colorLevels = 48`

16/32では同色に見える範囲にも実際の高低差が残ることがあったため、24/48へ細分化した。

---

## T3. カメラと状態機械

**完了済み。** スマホで1ホールを通して遊べる状態。

現在の状態機械:

```text
ADDRESS（読み＋方向調整） → STROKE → FOLLOW → CUP → RESULT → ADDRESS
```

旧 `READ → ADDRESS` の分離フローは廃止済み。

### ADDRESS

- 初期画面は方向調整
- 読みと方向調整を同一フェーズに統合
- 方向調整 / カップ後方 / 低い視点 / 横から、のボタンでカメラ切替
- カメラ切替をスワイプでは行わない
- 方向調整では約0.5mの短い直線ガイドを表示
- ガイドは打ち出し方向だけを示し、傾斜・曲がりを予測しない

### STROKE

- 真下視点
- カップ・旗竿は強制的に隠さない。近距離で自然に視野へ入れば見える
- 「方向調整に戻る」ボタンあり
- パターの表示角だけ感度0.6、時定数0.10秒で平滑化
- 実際の打ち出し方向には表示用平滑化を掛けない
- 待機中のパターはボール表面から約3px後ろ

### FOLLOW

- カメラ位置は固定し、ヨー・ピッチで追う
- STROKE画面上でボール中心が画面端の約82%へ来た時点でFOLLOWへ切替
- ボールが画面外へ消える前に現在位置へ即座に向ける
- 以後は時定数0.25秒で少し遅れて追従
- FOVは距離に応じて70° → 50°

### RESULT

- ボールが完全停止してから表示
- 軌跡を黄色線で表示
- カメラは**ほぼ真上（pitch 89.9° / yaw 0°）**
- 斜め45°俯瞰は旧仕様

---

## T3後の実機調整

### 確定済み

- ドット濃淡を `levels 24 / colorLevels 48` へ変更
- RESULTを真上俯瞰へ変更
- 旗竿を距離/FOVに応じて表示上だけ太さ補正
  - 基本半径12mm
  - 最大16mm
  - 低解像度画面で最低1.3pxを目安
- STROKEのパター待機位置をボール直後へ修正
- 補間時間のあるカメラ遷移を初期値のおおむね2倍へ変更
- CUP自動切替をカップまで約0.5mへ変更
- CUP固定視点でボールがフレームアウトする前にFOLLOWへ復帰
- STROKE中に「カップ確認」を追加
  - ボール真上の位置を保ったまま現在の狙い方向を見る
  - 左右スワイプで方向調整可能
  - 約0.5m補助線を表示
  - 30°傾き視点は実機比較後に**不採用**

### 比較実装・実機確認待ち

- ADDRESSのカップ後方視点だけ FOV 50°にして遠いボールの視認性を改善
- 補助線・RESULT軌跡
  - ドット版
  - 高解像度・半透明の滑らか版
- アンジュレーション
  - 標準 ±0.15m
  - 強調 ±0.20m
  - 標準の物理・色のまま形状だけ2倍
  - 強調の物理・色のまま形状だけ2倍
- パター反発倍率
  - 基準 `speedK = 0.00266`
  - 0.50×〜1.50×をスライダーで変更
  - ブラウザへ保存
  - 将来は練習モードでユーザーが合わせる想定

比較結果が決まるまでは、形状2倍や滑らかラインを正式採用扱いにしない。

---

## ゲーム全体の仕様検討（T4の前）

**次は別セッションでここを検討する。実装タスク番号はまだ付けない。**

決めたいもの:

- 何を目標にするゲームか
- 1プレイ/1ラウンドの構成
- ストローク数、タイム、スコアなど何を競うか
- 読み時間制限の有無
- 距離・グリーン難易度・ホール進行
- 練習モードとパター反発キャリブレーションの扱い

**仕様の検討はT4前、モード本体の実装はT4後**を基本とする。
グリーン形状・距離バリエーションも、このゲーム設計を決めてから具体化する。

---

## T4. 調整とデバッグ表示

**次の大きな実装タスク。ゲーム全体の仕様検討を先に行う。**

`docs/spec.md` §5を基準に進める。

主な内容:

- デバッグオーバーレイの整理
- `lil-gui` の調整項目を現在の実装に合わせて整理
- 指がボールを隠す問題の対策比較
  - (a) ボールを大きめにする
  - (b) 周囲にリングを出す
  - (c) タッチ位置を手前へオフセット
- 必要なら開発用表示を追加するが、本番UXへ持ち込むものは確認を取る

ここからは実機で触りながら数値を詰める。
ゲーム仕様・UXを変える選択は、選択肢と推奨案を出して確認してから実装する。

---

## T5以降

T4とゲーム全体の仕様が固まった後に番号を確定する。

候補:

- ゲームモード本体の実装
- ストロークモード
- タイムアタック
- 読み＋方向調整の持ち時間
- グリーン形状・距離・難易度バリエーション
- 見た目の仕上げ
- サウンドなどの演出

RESULTの軌跡表示はすでに実装済みなので、旧T5候補の「軌跡を芝に残す」は完了扱い。

---

# 進め方のルール

## セッションを分ける

タスクをまたぐときは新しいセッションを開く。
文脈は `CLAUDE.md`、`docs/spec.md`、この `TASKS.md` に残す。

## 仕様変更は記録も同期する

ゲーム仕様・UXを変更したら、実装確定後に `docs/spec.md` と必要な記録を現在状態へ合わせる。
比較中の機能は正式採用と混同しないよう「比較用」と明記する。
チャットだけに決定事項を残さない。

## 実機確認はユーザーが行う

操作感・見た目に関わる変更について、AIは「良くなった」と自己判断しない。
GitHub Pagesへ反映後、「実機で確認してください」と明示する。

## 詰まったら症状を具体化する

感覚だけでなく、可能なら状態・数値・再現手順を残す。
デバッグ表示はそのために使う。
